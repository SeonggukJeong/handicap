// Placeholder — full coverage comes from Task 14's RunDetailPage page test.
it.todo("useRunReport is enabled only when terminal && Boolean(id)");
it.todo("useRunReport uses staleTime: Infinity");
it.todo("useRunReport queryKey is ['runs', id, 'report']");
