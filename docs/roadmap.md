# Post-MVP1 로드맵 (data-driven 이후)

> **이 문서의 목적**: MVP1 슬라이스 1–6 + Slice 7(loop) + 7-1(loop breakdown) + Slice 8(data-driven 8a/8b/8c)이 모두 끝난 시점에서, **다음에 무엇을 할지 고를 때 가장 먼저 보는 단일 진입점**. 후보 슬라이스 + 각 슬라이스에서 의도적으로 연기한 자잘한 항목들을 한곳에 모은다.
>
> **언제 갱신**: 슬라이스를 하나 선택해 시작하면 그 슬라이스를 "진행 중"으로 표시하고, 끝나면 "완료"로 옮긴다. 새로 연기한 항목이 생기면 아래 §B에 출처 슬라이스와 함께 누적한다.
>
> **연관 문서**:
> - 후보의 원천 메뉴 = MVP1 spec **§4.5 "의도적으로 MVP 외 (다음 단계의 첫 후보)"** → `docs/superpowers/specs/2026-05-27-handicap-mvp1-design.md:513`
> - 기술 부채/소소한 후속(슬라이스 무관, batch 정리 대상) = `docs/followups-after-mvp1.md` (이 문서와 역할 분리: 저긴 *tech debt*, 여긴 *feature 로드맵*)
> - 결정 기록 = `docs/adr/`
> - 슬라이스 작업 흐름 = brainstorming → `docs/superpowers/specs/<날짜>-<이름>-design.md` → `docs/superpowers/plans/<날짜>-<이름>.md` → subagent-driven 구현

---

## 현재 상태 (2026-06-23)

- **✅ LAN 풀 제어상태 영속화 완료·master 머지 (2026-06-23, ADR-0041 후속·proto+controller+ui·migration 1[0019]·engine/worker-반응 0·byte-identical-off)** — L7 능동 제어(drain/capacity_override/label)가 in-memory 전용이라 컨트롤러 재시작(재배포·크래시·재부팅)이 풀을 비우면 사라져 정비 drain 워커가 재시작 후 *조용히* rotation 복귀하던 footgun을, 사용자 선택 **안정-식별자 모델**로 닫음(L7 "ephemeral 수용" 의도 연기분). 핵심 난점=재부착 키(기본 풀 worker_id는 프로세스마다 새 랜덤 ULID). **3 task**: ① proto `Register.stable_id = 6` additive + 워커 `stable = worker_id.is_some()`를 `connect_with_backoff`(reconnect.rs)→`connect_and_register`(client.rs)로 관통(`run`=false·`run_pool`=stable)·헬퍼 `worker_id_is_stable` — 컨트롤러 미반응=byte-identical 단독 ② migration 0019 `pool_worker_overrides`(`CREATE TABLE IF NOT EXISTS`) + `store/pool_overrides.rs`(get/upsert/delete) + `PoolEntry.stable` + `pool_register_idle`(stable이면 **락 전** get·Err→fail-soft·**INSERT 분기에서만** 재부착·UPDATE warm은 L7 R2 보존) + `pool_set_control`(락-밖 upsert/delete·반환 `bool`→`anyhow::Result<bool>`·patch_worker 200/404/**500**)·~38 호출부 arity churn·R14 락 규율 ③ `PoolWorkerInfo→Summary→Zod stable: z.boolean()` wire 1:1 + 대시보드 `!w.stable` "일시적" 인디케이터 + 익명 제어메뉴(drain/cap/label, exclude 제외) 미유지 힌트(`ConfirmDialog`/`EditModal` `hint?` prop)·`ko.workers.*`·RTL fixture 전수. STOP-gate 재개 **fresh /clear 구현**([[stop-gate-fresh-context-impl]])·subagent-driven 3 task `033dec3`/`98e4d22`/`a30b8f1`(per-task 리뷰 T1~3 **Opus 승격**: proto/와이어·migration/concurrency·REST↔Zod)+finish-docs·3 Minor accept-as-is. handicap-reviewer **APPROVE**(8 불변식 source 대조: wire 1:1 end-to-end·migration 멱등·apply-on-INSERT-only·R14 락·byte-identical-off·asymmetric 에러정책·pool_exclude 무변경·repo traps·"safe to ff-merge")·security-reviewer **APPROVE**(R12: 테이블 시크릿 0[parameterized·label 미로깅·React-escape·≤200]·DTO 누출 0[`token.is_none()` 회귀유지]·persistence operator-REST-only[worker stable_id는 register READ만 게이트]·재부착 self-limiting[drain=fail-safe]·trusted-LAN 무변경·DoS bounded). 게이트 green: nextest 726·clippy 0·UI lint 0/test 1052/build. **라이브(실 pool 2워커=stable `--worker-id w1` cap5+익명 ULID cap25·interval2/stale6/keepalive4)**: ① stable drain+override4+label="office-rack"→DB행 `w1|1|4|office-rack`→컨트롤러 kill+재시작(같은 DB)→3필드 재부착 ② 익명 drain→DB행0(byte-identical)→재시작 undrained 복귀 ③ 리퍼-evict(`kill -STOP`→stale6s 행 사라짐)→재접속→**가동 중** 재부착(R5②·L7 잠복갭 닫힘) ④ exclude w1(프로세스 exit)→DB행 생존→재실행 drain 재부착(R6) ⑤ Playwright anon "일시적" 배지+drain 다이얼로그 미유지 힌트·stable w1 배지/힌트 없음·Zod 0. 상세 → `docs/build-log.md`·ADR-0041 §귀결·[[windows-desktop-distribution]]. spec/plan `2026-06-23-lan-pool-control-state-persistence*`(R1–R12). **연기**: exclude 영속(denylist)·orphan 행 GC·worker 자가-id-파일·인증/RBAC(§A10)·일괄 동작·예약 drain·mutation-error toast·mTLS·per-stage 라이브니스.
- **✅ LAN 풀 운영 견고성 완료·master 머지 (2026-06-22, ADR-0041 운영 후속·controller+ui·migration/proto/engine/worker 0·byte-identical-off)** — L6 풀 하트비트 임계값(ping interval/stale timeout)을 재배포 없이 `/settings` 런타임 가변 + 리퍼 하드닝(try_send) + `/workers` 제어 에러 표면화. **5 task**: ① settings 레지스트리 3행(interval 1–3600·stale 2–86400 가변·keepalive readonly)+`check_heartbeat_pair`[stale>interval]를 PUT/DELETE/startup 교차필드 강제 ② `AppState` 하트비트 2필드 제거→settings 단일소스(대시보드 accessor·~50 literal byte-identical) ③ 리퍼 `try_send`[Full=skip/Closed=evict]+`interval.max(1)`+매 sweep settings 재읽기(런타임 적용) ④ `/settings` 3행+2× 여유경고+적용 안내 ⑤ `/workers` 제어 에러 인라인+undrain 배너+서버 `{error}` surface(capacity NaN/empty 가드 보존). fold-in: build() effective-쌍 클램프(오버라이드+시드 startup flapping 차단·사용자 'harden' 선택). subagent-driven 5 task `4f4aed2`~`d7a2171`(T1~3 Opus 승격). handicap-reviewer **APPROVE-WITH-NITS**·security **N/A**·게이트 green(nextest 718·clippy 0·UI 1044). 라이브(실 pool 2워커 짧은 임계값): last_seen 8s sawtooth로 런타임 재읽기·cross-field 400·startup clamp warn·byte-identical 기본 10/30/20·/workers capacity 범위밖 인라인 에러·Zod 0. spec/plan `2026-06-22-lan-pool-ops-hardening*`.
- **✅ LAN 분산 워커 L7 완료·master 머지 (2026-06-22, ADR-0041 후속·controller+worker-core+proto+ui·proto additive[Disconnect]/migration/engine 0·대시보드 제어 액션)** — 읽기전용 L2 대시보드(`/workers`)에 운영자 능동 제어 4종 추가: drain/undrain(새 배정 중단·진행 run 완주·되돌리기)·exclude(즉시 풀 제거+워커 깔끔 종료·busy run fail-fast)·capacity override(런타임 수동조정)·label(메모). **4 task**: ① `PoolEntry` 제어 3필드+`effective_capacity_vus()`(in-memory·migration 0)+`pool_register_idle` get-or-update(제어필드 보존·tx/hostname/capacity/last_seen 갱신·assigned_run 리셋 R2)+가드 3경로 `!drained`/effective+DTO 3필드 additive(R9) ② proto `Disconnect`(field 5 additive)+워커 두 사이트(idle-wait 루프·`forward_inbound` cancel param) Disconnect→프로세스토큰 cancel→재접속 없이 종료(R7) ③ `pool_set_control`(3-level Option 부분갱신·404)+`pool_exclude`(R14 락 규율: 캡처 락안·`worker_disconnected` fail-fast+`try_send` 락밖)+REST PATCH/exclude(capacity 1..=1M·label≤200·reason≤500·`deserialize_some`·404/400) ④ UI Zod `.nullable()`(S-D 갭)+mutations+대시보드 액션메뉴(kebab 단일-오픈)·"비우는 중" 배지·"N (수동)"·label 열·drain 되돌림확인창·exclude 차단형 alertdialog+busy 경고+편집 모달+RunDialog 프리뷰 `!drained && !busy`/effective(R4 parity)·전부 `ko.workers.*`(R12). **핵심 통찰**: worker_id 프로세스-안정→제어상태 reconnect 생존(in-memory 정당화); drain=컨트롤러 veto로 충분(exclude만 proto 필요); 프리뷰≡가드 한 규칙서 파생(R4); exclude busy=L6 `pool_disconnect` 재사용(신규 로직 0); 경고 카피=로그인부재 보완통제(R11/R13). **proto additive only·migration/engine 0·byte-identical-off**. STOP-gate 재개 후 **fresh /clear 구현**([[stop-gate-fresh-context-impl]])·subagent-driven 4 task `d89e08f`~`f2ccf31`+fold-in `6ac2d4a`/`d15af58`/`f9f65d7`/`4187a49`(T1~T4 code-quality **Opus 승격**·5 fold-in). handicap-reviewer **APPROVE-WITH-NITS**(4 불변식 source 대조: wire 1:1·R4 parity·R14 byte-identical+lock·R7 no-reconnect end-to-end; M1 never-fires·M2 fold-in·M3 defer)·security-reviewer **APPROVE**(6표면 clean: 시크릿 비노출·exclude 비-peer-triggerable·check_token 비우회·입력검증 fail-closed·무인증=기존자세·SSRF/injection 0). 게이트 green: nextest 709/709·clippy 0·UI 1044. **라이브(실 pool 2워커 cap[5,25])**: PATCH 200/400/404·reason캡400·deserialize_some clear(override→null·label 보존)·drain+override→`409 achievable=4`·exclude idle→프로세스exit·재등록0·exclude busy→run failed 단일terminal·Playwright(액션메뉴/배지/수동용량/drain확인/busy경고/RunDialog 프리뷰 유휴1대·총용량8VU·비우는중1대제외)·Zod 0. 상세 → `docs/build-log.md`·ADR-0041 §귀결·[[windows-desktop-distribution]]. spec/plan `2026-06-22-lan-worker-control-actions*`(R1–R14). **연기**: DB 영속 제어상태·인증/RBAC(§A10)·일괄 동작·자동 exclude·예약 drain·mutation-error toast(ops-settings 후속)·mTLS.
- **✅ LAN 분산 워커 L6 완료·master 머지 (2026-06-22, ADR-0041 후속·controller+worker-core+ui·proto/migration/engine 0·풀 워커 능동 하트비트)** — LAN 풀 라이브니스가 *gRPC 스트림 연결*만 봐 half-open(원격 절전·네트워크 파티션)이면 죽은 워커가 유령으로 남아 capacity 가드(L3~L5) 오계수·`/workers` 유휴 표시·busy면 영영 `running`. 하이브리드 해결 — (앱) 컨트롤러 리퍼가 풀 전원 `Ping` push + 워커 `Pong` echo + 임의 인바운드 `last_seen` 스탬프, `last_seen>stale`을 기존 `pool_disconnect`(idle 제거/busy `worker_disconnected` fail-fast·**신규 로직 0**)로 evict; (전송) tonic 서버+워커 Endpoint h2 keepalive가 죽은 *연결* teardown→reconnect→재등록. 기존 inert `Ping`/`Pong` proto 메시지 **활성화만**(proto 무변경). **4 task**: ① `PoolEntry.last_seen`+`pool_touch`+`pool_heartbeat_tick(now,stale)`(R13 주입형·R14 락 규율: 스냅샷 락 안·`.await` 0→drop→락 밖 send/evict)+핸들러 `pool_conn`-게이트 touch+가상시계 단위 6 ② 워커 idle-wait 단발→루프(Ping→Pong·Assignment break·legacy byte-identical)+`forward_inbound` `out_tx` Ping→Pong 펌프(`load_datasets`/`abort_listener` 무변경)+`Endpoint` keepalive 20s ③ main.rs CLI 3종(interval 10/stale 30/keepalive 20s)+리퍼 `tokio::spawn`(`is_pool_mode()` 게이트·move 전 clone)+gRPC 두 arm h2 keepalive+AppState 임계값 2필드(50 literal) ④ `PoolWorkerSummary.last_seen_secs_ago`+응답 래퍼 임계값 2종(Instant→경과초·token/env/tx 비노출 R12)↔UI Zod plain `z.number()`+`/workers` "마지막 응답" 열+stale 배지(`interval<secs_ago<stale`·임계값 응답서·roleless). **핵심 통찰**: "idle만 Ping"은 불안전(저활동 busy 워커 빈-배치 스킵 30s 침묵→거짓 evict)이라 idle+busy 전원 Ping·워커는 상시 `forward_inbound` 펌프 한 곳서 답(거짓 evict 0); 앱 하트비트(라이브니스/capacity 정합)와 h2 keepalive(연결 lifecycle/reconnect)는 관심사 분리(h2 없이 앱-evict만 하면 워커가 자기 evict 모름→재등록 안 함). **proto/migration/engine 0·byte-identical when off**(pool off→리퍼 미spawn·빈풀 no-op·legacy 풀 미등록). STOP-gate 재개 후 **fresh /clear 구현**([[stop-gate-fresh-context-impl]])·subagent-driven 4 task `1b17f81`~`6202584`+fold-in `bd9a32f`(배지 a11y/row-identity test·T1/T2/T3 code-quality **Opus 승격**). handicap-reviewer **APPROVE-WITH-NITS**(7 크로스커팅 불변식 source 대조: wire 1:1·R12 proto/migration/engine 0+시크릿 0·R11 byte-identical-off·R14 락 규율·compile-gate plumbing·R-id 커버리지·terminal-guard reuse; no Critical/Important·M5 interval-0 ship-as-is)·security-reviewer **APPROVE**(5표면 clean: 토큰 비누출·REST 시크릿 구조적 부재·auth 비우회·리퍼 evict 비-peer-triggerable·keepalive transport-only·SSRF/injection 0). 게이트 green: nextest 698/698·clippy 0·`--features bundle` build 0·UI 1037. **라이브(실 pool 2워커·짧은 임계값 interval 2s/stale 6s/keepalive 4s)**: ① 리퍼 spawn 로그 ② last_seen 8s간 1s 유지(Ping/Pong) ③ idle `kill -STOP`→badge window(3s,5s)→evict(행 사라짐·capacity 미계수) ④ `kill -CONT`→h2 teardown+reconnect→재등록 ⑤ busy `kill -STOP` mid-run→evict→run `failed`(영영 running 해소) ⑥ Playwright "마지막 응답" 열·"16초 전응답 없음" 배지(quiet row만)·Zod 0. 상세 → `docs/build-log.md`·ADR-0041 §귀결·[[windows-desktop-distribution]]. spec/plan `2026-06-22-lan-worker-heartbeat*`(R1–R14+R2b). **연기**: ops-settings 임계값 런타임 가변·리퍼 `send().await`→`try_send`(D1)·interval-0 `.max(1)` clamp(D2)·제어 액션·영속 worker_id·mTLS·per-stage 라이브니스.
- **✅ LAN 분산 워커 L5 완료·master 머지 (2026-06-22, ADR-0041 후속·controller+ui·closed-loop VU 곡선 과부하 가드 — LAN 가드 set 마지막 모드 완결)** — L4 가드를 **closed-loop VU 곡선(`vu_stages`) 풀 run**으로 확장(closed-fixed L3·open 고정/곡선 L4·closed 곡선 L5 = 4모드 완결). L4 접근자·2-param `reserve_idle_pool_capacity`가 곡선 peak를 *미리* 산출해, 가드 fork `!is_vu_curve()` 제외만 제거하면 예약/enqueue/register 무변경 재사용. 신규: ① `reduce_open_loop_profile`→`reduce_pool_profile` 개명+곡선 arm(각 `vu_stages[].target` `proportional_split` 비례 스케일·0-share 엔진 park·`max_in_flight` 미터치·R3 peak stage==`vu_count`) ② active-VU worker_id 머지(**migration 0018** `ensure_active_vu_worker_id`=A3b 0008 미러·read `SUM…GROUP BY ts_second`·N=1 byte-identical·worker_id `ReportJson` 미노출) ③ 가드 fork 곡선 포함+`?force` even-split(N=1→변경)+`validate` 254 `!is_pool_mode()` 게이트+예약-시점 `unique` 빈슬라이스 floor(`rows<n_pool` reject·3모드 공통 선재버그fix)+`release_pool_reservation`(unique-floor 예약-후/enqueue-전 거부 워커 누수 fix·beyond-plan) ④ RunDialog closed+curve 프리뷰 over-hint+409 곡선 다이얼로그(줄여진행=`scaleVuStages` 비례축소[vus0]·강행=force·취소)+부하 divergence 설명·`ko.capacityGuard` 곡선 변형. **proto/worker/engine 0·migration 1**·byte-identical(비-풀 곡선·단일워커 풀 곡선·closed-fixed/open/비-곡선·rows≥N unique)·의도적 동작변경 2(force 곡선 even-split·unique rows<N reject). STOP-gate 재개 후 **fresh /clear 구현**([[stop-gate-fresh-context-impl]])·subagent-driven 4 task `4f4d24c`~`a18e2a5`(T2 migration·T3 락 concurrency/요청실행/409/dataset code-quality **Opus 승격**). handicap-reviewer **APPROVED-WITH-NITS**(게이트 독립재실행 green: clippy 0·lib 283·L5 e2e 6·L3/L4 e2e 6·binding 9·UI 81; 7 seam 크로스커팅 검증·wire 1:1·release 배치·3 Minor 문서/robustness ship-as-is)·security-reviewer **APPROVE**(no findings·409 두정수만·`?force` check_token 비우회·worker_id ReportJson 미노출 이중방어·static-SQL idempotent migration·SSRF/템플릿 신규표면 0). **라이브(실 pool 2워커 cap[5,25]=30)**: ① 곡선 peak50→`409 {30,50}`+row0 ② peak28→201 fan-out·`active_vu_series` SUM머지 peak actual=28 ③ `?force` peak50→201 even-split actual<desired undershoot ④ peak1→N=1 byte-identical ⑤ unique rows1<2→400+워커 2/2 idle(release 작동) ⑥ Playwright(프리뷰 '총 용량 30 VU'·over-hint '곡선 최고점이 풀 유휴 용량 30 VU를 초과…'·409 곡선 다이얼로그 비례축소/강행 설명·scaleVuStages clamp 50→30[vus0·rps/slots None]·Zod 0). 상세 → `docs/build-log.md`·ADR-0041 §귀결·[[windows-desktop-distribution]]. spec/plan `2026-06-21-lan-distributed-workers-l5-closed-curve-capacity-guard*`(R1–R14). **연기**: 비-풀 곡선 fan-out(§B9)·L3/L4 다이얼로그 자세한-설명 소급·풀 unique 부족 시 capacity-비례 재분배(R14는 거부만)·곡선 active-VU CSV/XLSX·per-stage 워커 분해·measured rate capacity·제어 액션·mTLS·하트비트·다중 동시 run.
- **✅ LAN 분산 워커 L4 완료·master 머지 (2026-06-21, ADR-0041 후속·controller+ui·open-loop/곡선 과부하 가드)** — L3의 capacity 가드(closed-loop 전용)를 **open-loop(고정 `target_rps`+곡선 `stages`) 풀 run**으로 확장. 슬롯(`max_in_flight`)을 워커 `capacity_vus` 비례(`capacity_split`)로, 레이트를 슬롯에 비례 분배(고정=`proportional_split_min1`[각 워커 ≥1 — 엔진 `runner.rs:1093 .max(1)`이 0-rate 고정 워커를 1로 clamp해 초과 발사하는 것 차단]·곡선=`proportional_split`[0-share는 엔진 poll-흡수])하고, N을 `min(max_in_flight, rate_peak)`로 레이트-상한. `reserve_idle_pool_capacity` 2-param `(worker_cap, slot_total)`·`pool_achievable_capacity(worker_cap)`(예약과 같은 first-N 부분집합)·`reduce_open_loop_profile` 5번째 `slot_weights`·`spawn_run` 가드 fork `!is_vu_curve()`(open+closed)[coordinator+runs.rs] → RunDialog open 프리뷰/초과힌트(슬롯 워딩)+409 다이얼로그 mode 분기(open clamp=`max_in_flight`·target_rps/stages 유지·vus 0+안내문)·`ko.capacityGuard` open 변형(client.ts/hooks 0-diff·L3 `PoolCapacityError` 재사용). **proto/worker/engine/migration 0**·조건부 byte-identical(균등 cap+`rate_peak≥max_in_flight`→ADR-0038·closed·비-풀·`?force` 불변). **closed-loop VU 곡선(`vu_stages`) 풀 가드는 여전히 미적용**(N=1 legacy·under-cap 갭 존속=후속). STOP-gate 재개 후 **fresh /clear 구현**·subagent-driven 4 task `7399103`~`ad6b338`(T2 code-quality **Opus 승격**: 락 concurrency/요청실행+409 보안). handicap-reviewer **APPROVED-WITH-NITS**(게이트 독립재실행 green: clippy 0·shard 14/14·coordinator 43/43·runs 20/20·pool_open_loop e2e 5/5·RunDialog 75/75; wire 1:1 exact·byte-identical·min1/proportional 라우팅·R11/R12 deferral 코드+런북 양쪽·6 Minor 전부 ship-as-is)·security-reviewer **APPROVE**(no findings·409 두정수만·`?force` 가드만 우회 check_token 비우회·SSRF/템플릿 신규표면 0). **라이브(실 pool 2워커 cap[5,25]=30)**: R5 `409 {achievable_vus:30,requested_vus:40}`+row0 / R4 fixed rps29.9≈30 / R3 슬롯[4,4] rps15.98≈16 / R6 `?force` over-subscribe 201 rps39.9≈40 + Playwright(프리뷰 '총 용량 30 VU'·over-hint 슬롯워딩·409 alertdialog clamp→max_in_flight=30[target_rps 유지·vus 0]·Zod 0). 상세 → `docs/build-log.md`·ADR-0041 §귀결·[[windows-desktop-distribution]]. spec/plan `2026-06-21-lan-distributed-workers-l4-open-curve-capacity-guard*`(R1–R13). **연기**: closed-loop VU 곡선 풀 가드(N=1·별도 슬라이스)·capacity-비례 dataset `unique` 분할·measured rate capacity·제어 액션·mTLS·하트비트·다중 동시 run.
- **✅ LAN 분산 워커 L3 완료·master 머지 (2026-06-20, ADR-0041 후속·controller+ui·과부하 가드)** — capacity-aware closed-loop 풀 배정. 워커 `--capacity-vus` 존중 water-fill `capacity_split`/`achievable_capacity`(무바인딩=`shard_split` 균등 **byte-identical by construction**·0→`.max(1)`)[shard.rs] → `reserve_idle_pool_capacity`/`PoolReservation{Reserved,Insufficient}`(풀 락 1회 hold 안 스캔·비교·예약 원자·`.await` 0=TOCTOU 0·**빈풀 FIRST**[closed vus≥1이라 빈풀 achievable 0이 Insufficient/500 오라우팅 차단])+`enqueue` 5번째 `precomputed`/`register` precomputed-or-`shard_split` fallback[coordinator.rs] → `spawn_run` **insert-전** 사전검사 `409 {achievable_vus,requested_vus}`(run row 0=R3)+`?force`(ephemeral·가드만 스킵·auth 비우회)+모드 fork[runs.rs] → bespoke `createRun`+`PoolCapacityError`+`ko.capacityGuard`[client.ts] → RunDialog 총용량 프리뷰+초과힌트(closed+fixed만·role=status)+409 확인다이얼로그(줄여진행=clamp/강행=force/취소·`mutation.reset()` 재오픈가드·`!poolConflict` double-fire가드·`instanceof` 배너억제). **closed-loop만**(open-loop/곡선 미적용=후속)·**proto/worker/engine/migration 0**·조건부 byte-identical(전원 cap≥균등share or ?force or 비-closed or 비-풀). STOP-gate 재개 후 **fresh /clear 구현**(L2 '같은세션' 교정 반영·[[stop-gate-fresh-context-impl]])·subagent-driven 6 task `a7338c5`~`7e5be3a`+N1 docs fix `d0ba874`(T2/T3 code-quality **Opus 승격**: 락 concurrency/요청실행+보안). handicap-reviewer **APPROVED-WITH-NITS**(게이트 독립재실행 green: clippy 0·shard 10/10·coordinator 38/38·e2e 6/6·UI createRun 2/2·RunDialog 69/69·lint 0·build; wire 1:1·byte-identical·mode-fork·register-bounds[`expected==counts.len()` OOB 구조적 불가]·0-churn·N1 fix·4 Minor ship-as-is)·security-reviewer **APPROVE**(no findings·R12 PASS: 409 두정수만·?force auth 비우회·no-row·메타데이터 신규노출 0·injection/SSRF N/A). **라이브(실 pool 2×cap5)**: ① vus8→201 ② vus20→`409 {achievable_vus:10,requested_vus:20}`+row0 ③ vus10→201 ④ ?force vus20→201 과부하 ~354rps ⑤ 빈풀→400(409 아님) ⑥ 기본cap1000 vus20→201 byte-identical + Playwright(총용량 10VU 프리뷰·over-hint·409 alertdialog '풀 용량 부족'·clamp→vus10·force→vus20·Zod 0). 상세 → `docs/build-log.md`·ADR-0041 §귀결·[[windows-desktop-distribution]]. spec/plan `2026-06-20-lan-distributed-workers-l3-capacity-guard*`(R1–R12). **연기**: ~~open-loop/곡선 capacity 가드~~(✅ L4 2026-06-21)·capacity-비례 dataset 슬라이싱·제어 액션(disconnect/exclude)·mTLS·풀 워커 헬스/하트비트·다중 동시 run.
- **✅ LAN 분산 워커 L2 완료·master 머지 (2026-06-20, ADR-0041 후속·controller+ui+proto·읽기전용 가시성)** — L1 백엔드 풀을 비-CLI UI로: ① proto `Register.hostname` additive + 워커 `gethostname`(0.5) 송신 ② `CoordinatorState.pool_snapshot()` 읽기 접근자(`tx` 비노출) + `PoolEntry.hostname` ③ `GET /api/pool/workers`(off=빈 200·DTO에 token/env/dataset 0·R12) ④ `/workers` 대시보드(네비 '워커'·`usePoolWorkers` 폴링[함수형 refetchInterval: pool_mode면 3s 아니면 false]·hostname/유휴·Busy+run링크/capacity[선언값·미적용]·빈상태 2종·로딩 role=status/에러 role=alert) ⑤ RunDialog 풀-모드 프리뷰 배너("연결된 유휴 워커 N대"·비-풀 byte-identical) ⑥ 런북 §7/§7a(라이브니스=스트림·하트비트 없음·half-open 유령 워커). **migration 0/엔진 0**·조건부 byte-identical(pool_mode off+hostname 빈=pre-slice). subagent-driven 6 task `8f586cc`~`d56928b` + fold-in `60a1893`(로딩 스타일/테스트·RunDialog 프리뷰 mb-4 간격[사용자]·보안 nit 런북), T1–T3 backend code-quality **Opus 승격**. spec-plan-reviewer 3패스 clean APPROVE·handicap-reviewer **APPROVE**(wire 1:1 proto+REST↔Zod·R9·R12)·security-reviewer **APPROVE**(R12 시크릿 구조적 비노출·hostname React-escape XSS 안전·nit N1 무인증 LAN 메타데이터=신뢰-LAN 수용·mTLS L3). **라이브(Playwright, 실 pool 스택)**: 2워커 hostname·busy+run링크·프리뷰 '유휴 N대'(mb-4 간격)·워커 kill→폴링 내 사라짐·reconnect-per-run. **K8s 관계**: 대시보드/프리뷰는 `--worker-mode pool` 전용 — kubernetes 모드(per-run Job fan-out)는 워커가 풀 미등록이라 `/workers`가 "풀 모드 아님" 빈상태(두 모드 대안). 상세 → `docs/build-log.md`·ADR-0041 §귀결·[[windows-desktop-distribution]]. spec/plan `2026-06-20-lan-distributed-workers-l2*`(R1–R12). **연기(L3+)**: ~~과부하 가드~~(✅ L3 2026-06-20)·제어 액션(disconnect/exclude/cap)·정확한 N 프리뷰·mTLS·영속 worker_id·하트비트/last-seen·다중 동시 run.
- **✅ LAN 분산 워커 L1 완료·master 머지 (2026-06-20, ADR-0041·ADR-0039 LAN feasibility 후속·controller+worker+proto)** — 상시 워커 풀 백엔드 제어판. 세 번째 워커 모드 `pool`(subprocess/k8s 옆): 워커 `--run-id` 없이 띄우면 빈 run_id로 register→컨트롤러 인메모리 풀에 유휴 대기(reconnect-per-run), `--worker-mode pool` 컨트롤러가 run 발사 시 `spawn_run`에서 유휴 워커를 부하상한까지 `reserve_idle_pool`(풀 락 원자 예약)→`enqueue`→`assign_pool_workers`가 **기존 fan-out**(`register`/`assignment_for`/`stream_dataset`) 재사용해 샤드 push(N=min(유휴, 부하상한: closed `vus`/open `min(max_in_flight,target_rps|peak)`/curve 1)·per-worker capacity는 L1 무시[L2 가드]). proto `Register.token`(field 4 additive)+`check_token`(plain `==`, 미설정=accept-any opt-in) 공유 토큰 인증·`--rest`/`--grpc` 기본 127.0.0.1→0.0.0.0 LAN 바인드. **migration 0/엔진 무변경**·subprocess+토큰미설정 byte-identical·push=컨트롤러 권위. subagent-driven 6 task `0e4c6af`~`0da3fa4`(T1–T4 code-quality **Opus 승격**: wire/concurrency/gRPC/spawn·사전결정 T1 tautological 드롭·T4 실 predicate `should_run_pool`) + T5 리뷰fix `22ca6d2`(blind sleep→window-poll) + 보안fix `ca29a2a`(security-reviewer S1/S2: 시작 `info!(?args)`가 토큰 PSK 평문 노출→명시필드+`worker_token_set`/`token_set` bool). handicap-reviewer **APPROVE**(wire 1:1·byte-identical-off·migration-0·concurrency·R8 terminal-order·watchdog) + security-reviewer **APPROVE-WITH-FIXES** + **라이브 5/5**(use-all 2워커 count=146·토큰거부·빈풀400·재사용·report머지). 운영 런북 `docs/dev/lan-workers.md`. 상세 → `docs/build-log.md`·ADR-0041·[[windows-desktop-distribution]]. spec/plan `2026-06-20-lan-distributed-workers-l1*`(R1–R14). **연기**: ~~풀 상태 UI~~(✅ L2 2026-06-20)·워커 자동발견·과부하 capacity 가드·mTLS 기밀성·풀 워커 헬스/하트비트·다중 동시 run 멀티플렉싱. **세션 함정**: TDD-guard 키프얼라이브(plumbing src unblock·절대 commit 금지)·`<new-diagnostics>` E0061/E0004 STALE(독립 build로만 판정)·`execute_assignment` worker_id 파라미터(`RunAssignment`에 없음)·bundle 게이트 별도.
- **✅ Tauri 데스크톱 셸 완료·master 머지 (2026-06-19, ADR-0040·라이트 Windows 데스크톱 배포 옵션 B 접근 1·`desktop/` 신규+`docs/`·`crates/**`+`ui/src` 0-diff)** — bundle `controller` exe를 사이드카로 spawn해 네이티브 창에 띄우는 Tauri v2 셸(옵션 A 단일exe의 후속). 부팅→`127.0.0.1:0` spawn→controller 로그서 실제 REST 포트 파싱(**ANSI strip이 load-bearing** — `NO_COLOR`은 파이프엔 무력)→`/api/health`=`ok` 확인 후에만 창을 그 localhost로 navigate, 창 닫힘 시 `process_group`+killpg[Unix SIGTERM→SIGKILL]/Job Object[Windows KILL_ON_JOB_CLOSE]로 controller+손자 워커 트리째 종료. `ControllerBackend{base_url,shutdown}` 추상으로 접근2(in-process)·LAN 전방호환(R7)·`SpawnConfig` localhost 기본(R6)·`desktop/`는 자체 빈 `[workspace]`로 루트 워크스페이스 밖(R5)·`crates/ui` 0-diff(R4). subagent-driven 6 task `2109b1e`~`e1e16af`, T2 code-quality Opus 승격(spawn+동시성, Important 트리 누수 fix), handicap-reviewer **APPROVE**(R1–R14) + security-reviewer **APPROVE-WITH-FIXES**(`csp:null`=controller UI 자체 `<meta>` CSP·웹과 byte-identical→문서화 / `HANDICAP_CONTROLLER_BIN` 런북 노트, 둘 다 docs fix). **macOS 라이브 R2/R3/R8 ✅**(실 release bundle controller+손자 워커 killpg 고아 0·run 완료 rps 91·실 ANSI 파이프 포트 파싱)·R1 창 렌더/R9 WebView는 **GUI 세션 연기**(Windows 갭과 동일). 상세 → `docs/build-log.md`·ADR-0040·[[windows-desktop-distribution]]. 커밋 `2109b1e`~`e1e16af`. spec/plan `2026-06-19-tauri-desktop-wrapper*`(R1–R14). **연기(§7)**: 접근 2 in-process·코드서명/SmartScreen·네이티브 다이얼로그·트레이/자동업데이트·LAN 분산 워커·Windows CI. **세션 함정**: cargo 중첩 패키지 `[workspace]` 필요·`externalBin`이 `cargo build`도 바이너리 존재 요구·`.dmg`는 GUI 세션 필요(`.app` headless)·desktop 테스트는 cargo 게이트 밖(수동 `cargo test`).
- **✅ Zod 게이트-에러 한국어 매핑 완료·master 머지 (2026-06-19, ADR 불필요·ADR-0035/영역 U·U4 연기 항목 해소·`ui/`-only)** — 시나리오 에디터 검증 배너(`problems.ts::formatSegment`)가 영어 원문 fallback으로 흘리던 Zod 클래스를 한국어로 매핑: discriminator·enum(+received)·`.strict()` unrecognized-key·string `min(1)`·시나리오 트리 커스텀 컨테이너/숫자 6종(loop body·if/elif branch·parallel branches·branch steps·`repeat>=1`). 전부 `ko.editor.gate*` 카탈로그 경유(ADR-0035)·**`model.ts` 0-diff**(와이어/API/서버검증 byte-identical)·`normalizeList`로 허용값 정규화. 정규식은 실측 Zod 3.25.76 문구 단일 소스(기존 5분기 뒤 비충돌) + **실-Zod 드리프트 가드**(실 fixture→`parseScenarioDoc`→한국어 키 `toContain`, zod 범프 시 silent 영어강등 대신 red). 단일 green 커밋(3파일: ko.ts·problems.ts·problems.test.ts). STOP-gate 재개(전 세션 spec/plan→이 세션 stage 3)·subagent-driven 단일 task(implementer mid-task rest→continuation fix) + handicap-reviewer **APPROVE**(Opus 독립검증: en.js 문구 byte-match·non-shadowing 증명·1009 green·Minor 2 비차단) + security **N/A**(path-gate 매치 0). 라이브 waived(spec §6·표시텍스트-only·R9가 parseScenarioDoc 결정적 커버). 상세 → `docs/build-log.md`·[[gate-error-ko-mapping]]. 커밋 `4f03779`(+docs `3e265fd`). spec/plan `2026-06-19-gate-error-ko-mapping*`(R1–R10). **세션 함정**: ui-only plan src-편집(ko.ts)이 test-편집보다 앞이면 `tdd-guard`가 첫 src 편집 차단→test 파일 먼저 편집(pending RED diff).
- **✅ 에디터 test-run UX 정리 + 스킴-없는 URL 검출 완료·master 머지 (2026-06-19, ADR 불필요·영역 U/U4 범위 내·`ui/`-only)** — U4가 남긴 두 폴리시 갭 해소: (1) 에디터 헤더 중복 "미리 1회 실행" 버튼(숨은 기본값 발사·진입점 둘) 제거 → test-run을 아래 **단일 섹션**으로 통합·목적 자명화("시나리오 미리 테스트" 제목+안내), (2) 검증 배너가 `/`-prefix만 잡던 걸 **스킴-없는 모든 리터럴 host URL**(`example.com/api`·`localhost:8080/x`)로 일반화(변수-prefix는 false-negative-safe). `TestRunSection` `forwardRef`→평범 함수(헤더 `runNow` 핸들 dead)·공유 `testRunTitle` 키 컨트롤/결과 분리(R12). **R10 test-run 와이어 byte-identical**(`api/hooks.ts`·`api/schemas.ts` 0 diff, `TestRunPanel.tsx`는 :455 제목 키 1줄만). subagent-driven 3 task + 최종 handicap-reviewer **APPROVE**(Minor R11 단일-진입점 가드 소실 fold-in: `getAllByRole("button",{name:"미리 실행"}).toHaveLength(1)`) + security-reviewer **APPROVE**(path-gate가 `TestRunPanel.tsx` 매치로 트리거·라벨-only). 라이브 waived(spec §6 △·wire byte-identical·full page-mount RTL 커버). 상세 → `docs/build-log.md`·[[editor-ux-polish]]. 커밋 `062cbb4`~`33a3566`. spec/plan `2026-06-19-editor-ux-polish*`(R1–R12). **세션 함정**: plan 내부 모순(T2가 추가한 음수 단언 리터럴을 T3 grep-0이 금지)·보안 path-gate는 파일경로로 트리거.
- **✅ 스텝 템플릿 관리 페이지 + 삽입 시 변수 파라미터화 완료·master 머지 (2026-06-18, ADR 불필요·ADR-0036 범위 내·`ui/`-only)** — (A) 전용 `/templates` 관리 페이지(`EnvironmentsPage` 미러·목록/이름·설명 편집/읽기전용 미리보기/삭제·**생성 없음**[R5 의도적 비대칭]) + (B) 삽입 시 `{{flow}}`·`${ENV}` 토큰을 토큰별 *유지/이름변경/리터럴 치환*하는 2-phase **단일 Modal**(자동 추측 0·전부 기본 '유지'=silently-wrong 경로 제거). 순수 `templateParams.ts`(YAML Document 스칼라 visit·주석/구조 보존·identity byte-identical) + 신규 `scanEnvVars` + `InsertTemplateModal` 2-phase. 엔진/proto/controller/migration 0·무토큰/all-keep 삽입 byte-identical. handicap-reviewer **APPROVE-WITH-FIXES**(round-trip seam을 20개 적대적 리터럴로 실증·이중 Zod 게이트) + security-reviewer **APPROVE**(구조-안전 by construction·`badRename` 인젝션 차단·reserved-var shadow 불가). 라이브 waived(run/report/엔진 무관·spec §6·이중 게이트). 상세 → `docs/build-log.md`·[[step-template-management]]. 커밋 `17abfe6`~`bf3cec5`. spec/plan `2026-06-18-step-template-management*`(R-id 척추 R1–R15). **남은 §B10 QoL 후보** = ko.common 한국어화 일괄 롤업(아래 §B10).
- **✅ 응답기반 extract 작성 완료·master 머지 (2026-06-18, ADR 불필요·`ui/`-only, `d4a0000`)** — test-run 응답 패널의 JSON 본문 필드·헤더·Set-Cookie·상태를 클릭해 그 스텝 `extract`(엔진 4종 그대로)를 작성하는 UI-only 브릿지(§B10/§C 후속 8-1). 순수 `jsonPath.ts`(RFC9535·serde_json_path 0.7.2 lockstep)+`ResponseBodyTree`(스칼라 leaf만)+`ExtractConfirmRow`(평범 JSX)+`addStepExtract`(`setStepExtract` 재사용)+TestRunPanel 배선(back-compat byte-identical). 엔진/proto/controller/migration 0. handicap+security reviewer APPROVE, 라이브 R10 PASS(`$.data.token`+`$['weird.key']` 엔진 수용·토큰 다음 스텝 흐름). 상세 → `docs/build-log.md`·[[response-based-extract-authoring]]. **남은 §B10 QoL 후보** = 스텝 템플릿 관리 페이지·ko.common 롤업(아래 §B10).
- **✅ 옵션 A(단일 self-contained exe) 완료·master 머지 (2026-06-16, `bf20704`) / 📋 후속 후보 — 라이트 Windows 데스크톱 배포 (ADR-0039)** — K8s 없이 자기 Windows PC에서 소규모 테스트를 돌리는 "가볍게 쓰는 사용자"용 데스크톱 형태. **옵션 A 구현 완료**: cargo `bundle` feature(기본 off=byte-identical, CLI 표면까지)로 `controller` 단일 exe가 UI 임베드(rust-embed)·DB 사용자폴더(dirs)·워커 self-spawn(`current_exe` 멀티콜, run loop을 `handicap_worker` lib 추출)·브라우저 오픈(`--no-open`)·포트 자동회피. proto/DB/migration/리포트 무변경, macOS 라이브검증 PASS, 빌드 런북 `docs/dev/single-exe-build.md`, 상세 → `docs/build-log.md`. **남은 후속 후보** = 옵션 B(Tauri 래퍼·웹 UI 그대로 재사용·controller in-process 임베드)·exe 코드서명/인스톨러·LAN 분산 워커(아래)·Windows 크로스컴파일·CI 자동빌드(remote 미설정). Flutter/React Native는 거절(React Flow/Monaco/Zustand = DOM/웹, 전면 리라이트 강요). **관련 feasibility(추가 질문): LAN 분산 워커** — 다른 PC에 `worker --controller http://<ip>:8081`을 띄우면 pull 모델(ADR-0010)로 register→shard 실행이 *이미 가능*, 격차 3 = ① controller gRPC `127.0.0.1`→도달가능 인터페이스+방화벽 ② 원격 워커 기동 오케스트레이션(새 `RemoteDispatcher` SSH/WinRM/에이전트 or 수동, 현 per-run run_id-bake 한계) ③ 채널 mTLS(현재 인증 없음). 빌딩블록 = ADR-0019/0027/0038, 재아키텍처 아님. 스코프/구현 미결 — 시작 시 brainstorming→spec→plan. 상세 = `docs/adr/0039-windows-desktop-distribution.md`.
- **✅ HAR → 시나리오 가져오기 완료 (2026-06-16, ADR 불필요·ADR-0003/0013/0015 범위 내·`ui/`-only)** — 캡처된 HAR 파일을 handicap **와이어-형 시나리오 YAML**로 변환하는 클라이언트-온리 `/scenarios/import` 페이지. 사용자의 "스마트폰 트래픽 캡처→STEP 자동전환" 비전의 **1단계**(변환 코어 — 미래 라이브 캡처 앱과 공유될 재사용 자산을 먼저 검증; 라이브 프록시/MITM은 별도 미래 슬라이스). **3 task**: ① 순수 변환 코어(`ui/src/import/filters.ts`+`harToScenario.ts` — HAR `log.entries[]`→와이어-형 step, `newStepId`/`parseScenarioDoc`/`jsonBodyCastErrors` 재사용해 모델 fork 0) ② 페이지(`ScenarioImportPage` — 정적/호스트/요청별 필터·헤더모드 3종[all/strip-volatile/semantic-only, `:`의사헤더 전모드 제거]·이름유추·status assert·미리보기·복사) ③ 라우트+목록 진입 버튼+편집기 핸드오프(라우터 state `{importedYaml}` → `ScenarioNewPage` ref-가드 effect가 기존 `chooseTemplate` 1회 호출, StrictMode-safe·갤러리 byte-identical 회귀). **R2 척추(load-bearing)** = 출력은 **와이어-형**(`body:{json|form|raw}`·`assert:[{status}]`)이지 모델-형(`{kind,...}`) 아님(엔진 `Request` `deny_unknown_fields`) — `parseScenarioDoc`는 양형 통과(false-green)라 골든 리터럴(`json:`/`- status:`/`not kind:`)로 검증. json body는 `jsonBodyCastErrors` 비-빈이면 raw 폴백(에디터 authoring 게이트와 동일 함수 lockstep). **엔진·워커·proto·migration·백엔드 라우트 0**(머지 diff = `ui/` 한정). subagent-driven 3 task(task별 spec→quality 2단계 전부 APPROVED) + handicap-reviewer **APPROVE**(R2 와이어 1:1 엔진 serde 대조·`ui/`-only·exhaustive-deps·editor-seed 함정·jsdom footgun) + 리뷰 fold-in(R5-b/c 토글 RTL·M1 옵션 fieldset legend a11y·M4 빈-요청 안내·R8 목록링크 테스트·M4 json-parse-fail 테스트). **라이브 검증 waived**(spec §6 — `ui/`-only·run-생성/리포트-파싱/엔진 경로 무변경·S-D 갭 무관; R2를 골든 와이어-YAML + `parseScenarioDoc` 단위테스트로 닫음) + 머지 전 전체 `pnpm test` 922 green. 커밋 `d89a0c4`~`36aa8a6`. spec/plan `2026-06-15-har-scenario-import*`(R-id R1–R12). **연기(§7 + 리뷰)**: 라이브 캡처/프록시(별도 미래 슬라이스·그 큰 결정이 ADR 후보)·자동 그룹핑·env/`${BASE_URL}` 추출·dedup·비-HAR 포맷·응답기반 extract / **알려진 한계**: ① 비표준 메서드(TRACE/CONNECT) entry는 7-메서드 enum 밖이라 임포트 시 에디터 Zod 거부(graceful, 프록시 HAR엔 드묾 — 필터/힌트 추가는 follow-up) ② `navigator.clipboard` 미보안 컨텍스트(에어갭 HTTP)서 복사 무동작(핸드오프가 주경로).
- **✅ mean 지연 프록시 전면 업그레이드 완료 (2026-06-15, ADR 불필요·backend+UI, A9 스코프 C)** — 사이징 지연 프록시를 post-run(`insights.rs` `required`)·create-time(`SlotSizingHelper` 앵커 `usePriorOpenRunAnchor`) **양쪽 동시** p50→mean(우편향 `p50<mean` under-sizing + capacity-bound run의 slots 오분류 교정·parity 보존). `ReportSummary.mean_ms: u64`=`(overall.mean()/1_000).round()`(HDR µs→ms) 가산 + UI Zod `.strict()` 수용. R5 parity 코드레벨·R6 폴백(`mean_ms==0`, `l_sec>0` 가드 동형)·strict Zod↔serde·ripple 전부(Rust 4 컴파일 + 골든 fixture 1 런타임 + UI 픽스처 ~6). `ko.ts` 문구 "p50"→"평균 응답시간"(ADR-0035). **엔진·워커·proto·migration·p50/p95/p99·CSV/XLSX/비교 무변경**(순수 가산). subagent-driven 3 task + **spec-plan-reviewer 3라운드 clean APPROVE**(round1 1000× µs 버그·blast radius 잡음) + handicap-reviewer **APPROVE** + R3 라이브(mean_ms=55). **함께 머지: 프로세스 인프라** — spec/plan **R-id 척추 템플릿**(`_TEMPLATE.md`) + `spec-review-guard`(브랜치 plan에 `REVIEW-GATE: APPROVED` 마커 전엔 src 편집 deny = spec-plan-reviewer 루프 강제, 부분문자열 엣지 차단). 커밋 `b84ceb5`~`b740680`. spec/plan `2026-06-15-mean-latency-proxy-upgrade*`, dogfood 노트 `_template-dogfood-notes.md`, build-log 상세.
- **✅ XLSX Insights 사이징 3열 완료 (2026-06-15, ADR 불필요·backend-only)** — A9 `load_gen_saturated` 인사이트의 사이징 처방(`recommended`/`cause`/`recommended_workers`)을 단일-run XLSX `Insights` 시트에 노출(화면 `InsightPanel`엔 이미 뜸·export엔 빠져 있던 갭 — A9 사이징 권장 + ADR-0038 worker_count 추천 두 선행 슬라이스가 의도적으로 연기). 순수 가산·읽기경로(`crates/controller/src/export.rs` 한 파일: `report_to_xlsx` Insights 헤더 9→12 col 9/10/11 + 행 writer conditional `if let Some` 3줄 + calamine 라운드트립 테스트 확장), 데이터는 이미 `ReportJson.insights[]`에 존재해 **새 계산 0**. 엔진·워커·proto·migration·`Insight` 구조체·`derive_insights`·`report.rs`·UI/Zod·ko.ts·CSV·비교(multi-run) XLSX·사이징 수식(p50 parity) 전부 무변경(append-only). subagent-driven 1 code task + handicap-reviewer **APPROVE**(spec §3.1/§3.2/§3.3 정확·§4 무변경 불변식·col/행 math·빈-셀 불변식), 게이트 green(test 1·clippy 0·pre-commit 전체 워크스페이스). 라이브 불요(spec §5: run-생성/report-파싱/엔진 경로 무변경, calamine 라운드트립이 직렬화 계약). 커밋 `f45eb13`. spec/plan `2026-06-15-xlsx-insights-sizing-columns*`. 연기 → §A9: mean 프록시 전면 업그레이드(C)·per-window dropped·원인 자동 귀속·CSV/비교 인사이트.
- **✅ CSV / Compare 인사이트 표면화 완료 (2026-06-15, ADR 불필요·ADR-0028/0030 범위 내·읽기경로)** — A4c/B7가 의도적으로 연기한 "CSV/compare 인사이트" 갭 해소(위 XLSX 슬라이스 연기 "CSV/비교 인사이트" 포함). 이미 계산돼 `ReportJson.insights[]`에 있던 인사이트를 버리던 4표면(① 단일-run CSV `GET /api/runs/{id}/report-insights.csv`, ② 비교 CSV `GET /api/scenarios/{id}/runs/compare-insights.csv`[long-format, `run_id` prepend], ③ 비교 XLSX `Insights` 시트, ④ 비교 화면 `InsightCompareMatrix`[kind×run])에 노출 + 부산물로 단일 XLSX `onset_second` 열(A9 cause-attribution 후 누락분) 추가. **척추 = 정규 컬럼 셋 단일 소스**(`INSIGHT_COLUMNS: [&str;13]` + 공유 행-writer `insight_csv_cells`/`write_insight_xlsx_row`) → 4표면 컬럼 드리프트 구조적 불가(R5). **신규 계산·와이어 0**(R7 — 매트릭스는 클라가 이미 fetch한 `/report.insights` pivot만, schemas.ts/proto/engine/worker/migration·기존 4 export byte-identical). 순수 UI/export 가산(`export.rs`·`api/runs.rs`·`app.rs`·`client.ts`·`ReportView`/`ScenarioComparePage`·신규 `InsightCompareMatrix.tsx`·`ko.ts`). subagent-driven 5 task(task별 spec→quality 2단계 전부 APPROVED) + handicap-reviewer **APPROVE**(R5 4표면 parity·R9 byte-identical·R7 와이어0·라우트 트랩) + UI 일관성 Minor 2 fold-in(배지 음영 InsightPanel 일치·열 라벨 부모 runLabels lockstep·severity 한국어 ADR-0035). **라이브 PASS**(50ms responder·완료 run 2개: 단일/비교 CSV 클린 파싱·비교 XLSX Insights 시트·매트릭스 `가장 느린 스텝·ping`=`정보 56`/`SLO 통과`=`정보`·콘솔 Zod 0). 커밋 `1c08c16`~`ebb76d6`. spec/plan `2026-06-15-csv-compare-insights*`(R-id R1–R12). 연기 → §A4c/§B7: 비교 화면 run별 InsightPanel 나열·회귀-인식 diff·CSV 스키마 정규화.
- **✅ open-loop worker_count create-time 사이징 헬퍼 완료 (2026-06-15, ADR 불필요·순수 UI)** — 방금 머지된 ADR-0038 멀티워커 fan-out의 후속(§8 연기 ① 해소). RunDialog **열린 루프**(open, 고정·곡선) worker_count 접이식 disclosure 안에서 run을 돌리기 전에 "권장 워커 수"를 미리 답하는 **하이브리드 3-tier** 헬퍼(`WorkerSizingHelper`). 근본 제약 = 워커당 RPS 천장은 *포화(드롭) 시에만* 관측되므로(다른 3종 헬퍼의 rate-independent 계수와 다름), tier로 confidence를 갈음: **A. 강한 근거**(prior open-loop run `dropped>0`) = tight 권장 + "측정된 천장" / **B. 약한 근거**(`dropped==0`) = 보수적 상한 + "포화시켜 측정" 안내 / **C. 근거 없음**(prior open run 없음) = "1대로 시작" 안내·적용 버튼 없음. 권장식 `N = ceil(target × prior_wc / peak)`(floor 1, 상한 64 클램프)는 사후 `load_gen_saturated`(cause=capacity)의 `recommended_workers`(`insights.rs:250`)와 **수식 1:1**. 앵커 = prior run 리포트의 **count 기반** peak(`peakThroughput` = 초별 Σcount 최대, `insights.rs:214-222` `by_sec`와 동형) — p50 기반 슬롯 헬퍼와 달리 localhost sub-ms run에서도 앵커가 산다. cross-field 비차단 경고 2종(하드캡 64 초과·`max_in_flight` 미달 `(으)로` 병기). 순수 함수 `peakThroughput`/`recommendWorkers`(`sizing.ts`, 기존 `targetRpsValid` 재사용) + 자족 `WorkerSizingHelper`(co-located `usePriorOpenRunWorkerAnchor`, env/measure 경로 없음) + `LoadModelFields` optional `onApplyWorkerCount` 게이트(worker_count disclosure는 rateMode 분기 *앞*의 **단일 공유 블록** = 슬롯 헬퍼 per-arm과 다름·한 번만 렌더, RunDialog만 전달→ScheduleForm 부재) + `ko.workerSizing`. **엔진·워커·proto·controller·migration·Zod 와이어 무변경, run 생성 페이로드 byte-identical**(머지 diff = `ui/` 한정). 라이브 PASS(3 tier 실서버 데이터 + 곡선 peakBased + cross-field + 콘솔 Zod 0). 상세 §A9·build-log. spec/plan `2026-06-15-worker-count-sizing-helper*`.
- **✅ open-loop 멀티워커 fan-out 완료 (2026-06-15, ADR-0038)** — open-loop(arrival-rate)을 단일워커 v1에서 계획된 멀티워커 fan-out으로 확장. 명시 `worker_count` 노브(기본 1, open 전용) + 컨트롤러가 register 시 워커별 `target_rps`·각 `stage.target`·`max_in_flight`를 `shard_split`로 분할(합=총량) → **엔진·워커·proto·migration 무변경**, A3a N-spawn·A3b 메트릭/dropped 머지 재사용. 검증 5종(open-loop+vus>0 거부 = `validate_run_config` 공유 게이트라 발사/저장 시 재검증), 사후 `load_gen_saturated`에 `recommended_workers`(per-worker 정규화 `ceil(target×wc/peak)`) 가산, RunDialog open 모드 접이식 worker_count 입력. N=1 byte-identical. 라이브 PASS(워커당 400rps 고정: 1워커 395 vs 2워커 790=2×·메트릭 머지 / 포화: recommended_workers=6 / UI safeParse). 상세 §A3·build-log, ADR-0038.
- **✅ Closed-loop VU 곡선 완료 (2026-06-13, ADR-0037)** — k6 ramping-vus 스타일 VU 수 시간별 변화(vu_stages + ramp_down 노브). 격리 함수 `run_scenario_vu_curve`(park-gate: 250ms tick 슈퍼바이저 + watch 채널 + per-VU child 토큰 슬랩) + proto field 12(vu_stages)/13(ramp_down) additive + 마이그레이션 0(profile_json serde default) + UI RunDialog closed+curve 활성화(stage 에디터·ramp_down 선택기·LoadModelFields 배선). 기존 closed/open-loop byte-identical. 단일워커 v1(max VU > capacity → 400). 상세 §D.
- **✅ Run 스케줄러 완료 (34a+34b 백엔드 2026-06-06, 34c UI 2026-06-07, ADR-0034)** — 예약(once)/반복(cron) 자동 발사. 컨트롤러 내장 `run_scheduler` 루프 + migration 0011(`schedules`/`schedule_events`) + `api/schedules.rs` CRUD/preview-next/events. **34c(UI) 머지 완료** = `/schedules` 페이지·5모드 트리거 빌더·라이브 preview-next·`profileForm`/`<CriteriaFields>` 공유 추출(RunDialog byte-identical). 엔진·워커·proto·migration 무변경. 상세 §A7.
- **✅ A2 Parallel 노드 완료 (2026-06-06, ADR-0033)** — 동시 분기 fan-out(`join_all` wait-all + `{{branch.var}}` key-origin merge). 상세 §A2.
- **✅ B7-C 레이턴시 단계 분해 완료 (2026-06-07)**: TTFB(=기존 `latency`, byte-identical 유지) + 본문 다운로드 2 phase 분해, opt-in(`Profile.measure_phases`). A2-2 `group_stats` 파이프라인을 `(step_id, phase)` 키로 미러 — `MetricBatch.phase_stats=8`/`Profile.measure_phases=11`/migration 0013 append-only(PK 없음)/`build_report` 7번째 param 별도 `download_acc`(summary 비오염)/Zod 3종 분기(measure_phases=`.default(false)`, download=`.optional()`, trace download_ms=`.nullable()`). off=byte-identical, e2e smoke green, 라이브 TTFB/다운로드 분리 확인. ADR 신규 불필요(ADR-0017/0033 범위 내 additive). 연기: DNS/TCP/TLS/total phase(reqwest 0.12 keep-alive, 저가치) + 기타 §B7-C 항목.
- **✅ 영역 U (UX 초보자 친화 재설계) 완결 (ADR-0035, 2026-06-12)**: 전 여정(홈→에디터→run→리포트) UX 개선 프로그램. 철학 = 단일 UI 점진 노출(C) + 홈 가이드 레이어(체크리스트 카드+템플릿 갤러리), 한국어 우선 + `ko.ts` 카탈로그. **U1a**(카탈로그+HelpTip+용어사전) + **U1b**(RunDialog 3그룹 재구성+chips+한국어화+막힘 사유) + **U2**(홈 온보딩 가이드 카드+빈 상태 5종+네비 한국어화+breadcrumb 5페이지, 2026-06-11) + **U3**(템플릿 갤러리 4종+에디터 진입 장벽: url 완화·재라벨·⚠ 배지·변수 치트시트, 2026-06-11) + **U5**(리포트 해석: 쉬운 요약 헤드라인+표면 한국어화·HelpTip 잔여+인사이트 행동화+running 진단+FAIL popover, 2026-06-12) + **U4**(에디터 검증 피드백 배너 2계층 + test-run 헤더 "미리 1회 실행" 승격, 2026-06-12 — 마지막 슬라이스) 전부 머지. 상세 §A8.
- **✅ Parallel per-branch breakdown 완료 (2026-06-12)**: 각 `parallel` 노드 아래 분기별 레이턴시 분포(count+p50/p95/p99/max_ms)를 페이지 행과 병치 — "어느 동시 분기가 병목인가" 가시화. A2-2 `group_stats`를 `(step_id, branch)` 키로 확장(proto `GroupStat.branch=4`·migration 0014·`GroupLatency.branches`·UI ↳ sub-행), 비-parallel run·페이지 행 byte-identical, ADR 신규 불필요(ADR-0033 범위). 상세·연기 항목 §A2.
- **완료(2026-06-05)**: **subprocess 워커 비정상 종료 fail-fast** (구 followups 열린 항목 A, master `ea714d4`) — A3a `worker_disconnected`가 "영영 running"은 이미 해소했고, 잔여 갭(register 전 사망 60s 정체 + message=NULL)을 reaper `mark_failed_if_active`(terminal 비클로버 가드)로 즉시 처리. 컨트롤러 전용, 엔진·워커·proto·마이그레이션 무변경. **→ `followups-after-mvp1.md` 열린 항목 = 없음.**
- **완료(2026-06-23)**: **실패 run 진단 사유(message)** (G3, 위 fail-fast의 status-transition 갭 후속 정리) — coordinator의 세 Failed 전이(`record_phase` 워커-보고·`worker_disconnected`·`fail_incomplete_registration`)가 `message=NULL`이던 잔존 갭을 닫음: 셋 다 `mark_failed_if_active`로 교체 + `record_phase`가 워커 `RunStatus.message`(엔진 실제 에러) 관통 → 모든 Failed run이 non-null 사유 message. controller+docs only, proto/worker/engine/migration/UI 0·비-Failed byte-identical. spec/plan `2026-06-23-run-failure-reason-message*`, 상세 build-log. **잔존 후속**: G1(아래에서 G1a 완료), G2(k8s register-전 사망 reaper, 현재 60s watchdog 폴백).
- **완료(2026-06-23)**: **Run 진행 라이브니스 — 등록 후 hung 워커 (G1a: startup-A + backstop-B)** — G3 직후 유일 잔존 stuck-running(워커가 등록·gRPC 건강한데 엔진 무진행: 0 메트릭·영영 `running`)을 닫음. 진행 신호=마지막 메트릭 도착(`ingest_metrics` first_load cancel). 기존 `registration_watchdog`→3-phase `run_watchdog`(등록→**A** startup 무부하 grace→**B** backstop 예상종료 초과) 자동 `Failed`+사유. `done` 토큰이 모든 finalize cancel→healthy byte-identical(영영 running이던 run만 Failed). B 예상시간=`run_duration_secs` proto 추출(워커·컨트롤러 단일소스). grace=`watchdog_grace` `Arc<OnceLock>` CLI(`--run-startup-grace-seconds` 90·`--run-backstop-grace-seconds` 120). controller-only, migration/proto-field/UI/settings/AppState/engine 0. handicap-reviewer APPROVE-WITH-NITS·security N/A·nextest 742·라이브(subprocess+`kill -STOP`) A/B/healthy 3종 통과. spec/plan `2026-06-23-run-progress-liveness*`, 상세 build-log. **잔존 후속**: **B2**(A/B/C 임계값 /settings 런타임 가변, OnceLock→settings 이주), **G2**(k8s register-전 사망 reaper).
- **완료(2026-06-23)**: **Run mid-run stall advisory (G1b: C — mid-run stall)** — G1a 3-tier의 마지막. running run의 마지막 메트릭이 120s 침묵하면 RunDetailPage에 advisory 배너+[중단] 버튼. 순수 클라(`ts_second` wall-clock·`silence=floor(now/1000)−max(ts_second)`)·`computeRunStall` 헬퍼가 startup+midrun을 단일 `stall.kind`로 통합·advisory라 자동fail 안 함(think-time 침묵과 구분불가·B가 최종 차단). 백엔드/proto/migration/`schemas.ts`/engine 0·UI-only·healthy byte-identical(status 절대 불변). subagent-driven 2 task(`5c1fbcf` computeRunStall+10 단위테스트·`e07f770` 배너+중단+ko+RTL 3)·handicap-reviewer APPROVE-WITH-NITS·security N/A·UI lint 0/1065 테스트/`tsc -b`+build·라이브검증 생략(spec §9 — UI-only·schemas.ts 무변경=S-D Zod 갭 없음). spec/plan `2026-06-23-run-stall-advisory*`, 상세 build-log. **잔존 후속**: run 목록 배지+서버계산 stall·B2(임계값 /settings 가변)·think-time 도출 임계값.
- **완료**: 슬라이스 1–6 (MVP1 전부) + Slice 7 (loop 노드) + Slice 7-1 (loop_index별 요청수 breakdown) + Slice 8a/8b/8c (data-driven 전체) + **Slice 9a/9b/9c/9d (Conditional `type: if` 노드 전체 — 분기별 메트릭 breakdown 포함)** + **영역 A (Run 프리셋 + Retry, ADR-0024)** + **영역 B (환경/Environments, ADR-0025)**. 열린 9 이전 슬라이스 작업 없음.
- **완료**: **A3 (멀티 워커 fan-out)** — **A3a(조정 인프라) + A3b(메트릭 머지) + A3c(K8s Indexed Job + dispatcher cleanup 배선 + Helm) 전부 구현+머지 완료(2026-06-02, subagent-driven) → 영역 A3 완결**. 계획된 fan-out(반응형 HPA 아님), A3a(조정)→A3b(메트릭 머지)→A3c(K8s Indexed Job) 3분할. spec `docs/superpowers/specs/2026-06-01-multi-worker-fanout-design.md`, ADR-0027. 상세 §A3. 연기(§B2''): per-deploy 워커 resource Helm values, 반응형 HPA, best-effort/degraded, `unique` 바인딩.
- **완결**: **영역 C (시나리오 에디터 test-run, A6 후속·spec §7 실현)** — **C-1(백엔드) + C-2(UI) 둘 다 구현+머지 완료(2026-06-01, subagent-driven)**. C-1: 컨트롤러 in-process 엔진 trace(`trace_scenario`) + top-level `POST /api/test-runs`(ephemeral) + `render_collecting`/`execute_step_traced`/`select_branch` 추출 + `ApiError::Unprocessable`(422) + if/elif 조건 미바인딩 변수 결정 행 표시(follow-up 확장). **C-2(UI)**: ScenarioTrace Zod 스키마(엔진 `trace.rs`와 와이어 1:1) + `api.createTestRun`/`useTestRun`(ephemeral 무invalidation) + 프레젠테이셔널 `TestRunPanel`(http/if 행·분기 라벨·`#loop_index`·미바인딩 앰버·truncated 배너·펼침 req/resp) + `ScenarioEditPage` 배선(`<EnvironmentPicker>`/`resolveEnv` 재사용, live `yamlText` 버퍼 전송 — 미저장에도 동작). spec §5-1 if 행 **조건 요약**은 `summarizeCondition`을 `CanvasView`→`scenario/model.ts`로 추출 공유하고 `ScenarioEditPage`가 `parseScenarioDoc(yamlText)`로 `steps`를 주입해 렌더(ScenarioTrace엔 조건 텍스트 없음). 5커밋(4 plan task + 조건요약 follow-up), task별 2단계 리뷰 + 최종 handicap-reviewer READY-TO-MERGE(와이어 1:1 검증), UI 294 pass + `tsc -b` clean. **후속(연기)**: 응답기반 extract authoring(§8-1), 수동 변수 오버라이드(§8-2), 워커경로 runner(§8-3), 민감값 마스킹, test-run 히스토리. spec: `docs/superpowers/specs/2026-06-01-scenario-editor-test-run-design.md`, plan: `docs/superpowers/plans/2026-06-01-scenario-editor-test-run-c2-ui.md`.
- **완결**: **A4 영역 (LoadRunner급 리포트 깊이)** — **A4a(run-level SLO verdict, ADR-0028) + A4b(run 비교 + 리포트 CSV/XLSX export, ADR-0030) + A4c(actionable 리포트 요약, `insights.rs` 결정론적 인사이트 6종 + XLSX Insights 시트 + UI InsightPanel) 전부 구현+머지 완료(2026-06-03, subagent-driven)**. A4b는 하이브리드(클라 비교/서버 export), A4c는 백엔드 `derive_insights`→`ReportJson.insights`. 세 슬라이스 모두 엔진·워커·proto·마이그레이션 무변경. 상세 §A4.
- **완료**: **JSON body 타입 캐스트 주입** (post-MVP1, ADR-0029, 2026-06-03) — flow `{{var:num}}`/`{{var:bool}}`(+`:str`) 캐스트 토큰으로 JSON body 문자열 leaf를 number/bool로 coerce. 순수 단일 토큰 leaf만, leaf 레벨 파싱(`cast.rs`, `template.rs` 무변경), 엄격 실패=`CastFailed`/trace=문자열 best-effort, UI Zod `.superRefine` 검증. 캐스트 없으면 byte-identical. 상세 §B1.
- **완료**: **Header/Form 벌크 입력** (2026-06-01) + **B4 disabled 행 토글** (2026-06-03) — 사용자 요청 QoL(순수 UI 영역). KeyValueGrid 2열 그리드 + BulkEditPanel(Postman 전체교체) + commonHeaders 피커 + JSON body Format 버튼 + Postman식 disabled 행 체크박스(`Request.disabled` 사이드카, executor 무시·byte-identical). 상세 §B4.
- **완료**: **codex 평가 후속 항목 1–4** (2026-06-03, master `5e59048`/`fffec3e`/`fee8041`) — lint 게이트 구멍 + dispatch fail-fast(P0: run failed+message+5xx) + dispatcher 테스트 + graceful-shutdown 로그. 잔여 5(open-loop·http_timeout)·6(skip/todo UI 테스트) → §B5.
- **완료**: **영역 D / S-A (타임아웃)** — run-level `http_timeout_seconds`(profile, 30s 하드코드 대체) + per-step `timeout_seconds`(HttpStep 오버라이드) 구현+머지 완료(2026-06-03, subagent-driven 5 task, master `c5c3f27`→`8ff689d`). 마이그레이션 0건(profile_json serde default), absent→byte-identical, connect_timeout 의도적 제외(연기). wire 1:1 7-layer, handicap-reviewer READY-TO-MERGE. 상세 §D.
- **완료**: **영역 D / S-B (think time)** — run-level(`Profile.think_time`) + per-step(`HttpStep.think_time`) 지연 + 사용자선택 시드(`think_seed`) + test-run `apply_think_time` 토글 구현+머지 완료(2026-06-04, subagent-driven 10 task, master `8ff689d`→`0dda5df`). 신규 `engine/src/pacing.rs`, 인터프리터 레이어 페이싱(executor byte-identical), 마이그레이션 0건, absent→byte-identical, 메트릭/proto 무변경, handicap-reviewer READY-TO-MERGE + 라이브 수동테스트 PASS(RPS 7341→9.99/13.3, test-run off=2ms/on=804ms, UI 3표면 round-trip). 영역 D 잔여: S-C(open-loop, ADR-0031)/S-D(stages). 상세 §D.

---

## A. 후보 슬라이스 (spec §4.5 메뉴)

우선순위가 아니라 후보 목록이다. spec §4.5의 한 줄을 각 후보에 그대로 매핑하고, 이 코드베이스 기준의 착수 메모를 덧붙인다.

### A1. Conditional 노드 — **완료 (9a+9b+9c+9d 전부 머지)**
- **spec 근거**: §4.5 "HTTP 외 다른 노드 종류 (loop, **conditional**, parallel, …)".
- **성격**: control-flow 노드. Slice 7 loop가 깐 인프라(재귀 `Step` 트리, internally-tagged enum `#[serde(tag="type")]`, `execute_steps(steps, ctx)` 재귀 인터프리터, React Flow 컨테이너 노드)를 그대로 재사용하는 가장 자연스러운 다음 수.
- **참고**: ADR-0020 (control-flow 노드: loop 패턴). spec §2 모델 노트 "후속 단계의 `loop`/`conditional` 노드는 모델에 `type` 추가, 내부 `do: [...]` 중첩 — 캔버스에서는 컨테이너 노드로 시각화" (`...mvp1-design.md:287`).
- **결정·진행**: spec `docs/superpowers/specs/2026-05-30-slice-9-conditional-node-design.md`, ADR-0023. 4분할 전부 완료 — **9a(엔진: 평탄 if/elif/else + 재귀 조건 트리 + lenient 평가)**, **9b(UI authoring: Zod `ConditionModel`/`IfStepModel` + 캔버스 if 컨테이너 + 재귀 조건 빌더 inspector)**, **9c(상호 1레벨 중첩 if↔loop: two-tier Zod 게이트 + 재귀 캔버스 에미터 + `findStepById` + 게이트 중첩 버튼)**, **9d(분기별 메트릭 breakdown: `run_if_metrics` + `ReportJson.if_breakdown` + `BranchStatsTable` — Slice 7-1 패턴 재사용, cap/sentinel 없음, counts-only, 최상위 배열)**.

### A2. Parallel 노드 — **✅ 완료 (구현·머지 완료, ADR-0033, 2026-06-06)** + **A2-2 그룹/페이지 레이턴시 ✅ 완료 (2026-06-06)**
- **완료 (2026-06-06, subagent-driven 9 task = P-a 엔진 4 + P-b UI 5)**: `type: parallel` 한 VU가 분기를 `futures::future::join_all`로 동시 실행(wait-all, 공유 cookie jar/client ADR-0018) + 분기 출력 `{{branch.var}}` key-origin 네임스페이스 merge-back(선언 순서) + top-level-only·http-only 분기(two-tier Zod gate) + trace 순차·동일 merge + insights `collect_unconditional` 1 arm + 캔버스 세로 레인(`ParallelStepNode` + `measureWidth`) + Inspector 분기 CRUD. **엔진(`scenario`/`runner`/`trace`)+`controller/insights.rs`+UI만 — proto·워커·migration·메트릭(per-step 재사용) 무변경, 핫 flat-http byte-identical.** task별 2단계 리뷰(spec→code-quality) + 최종 handicap-reviewer READY-TO-MERGE(와이어 1:1 양방향). 라이브 검증: 부하 run 40req/0err·`/a`·`/b` 동일 타임스탬프(동시성)·p50≈309ms(max not sum)·`/combine?u=AAA&f=BBB`(네임스페이스) + test-run trace `final_vars{a.id,b.id}`. **잔여(아래 (3) + §B 누적)**: per-branch breakdown·중첩·open-loop 상호작용·분기 fail-fast.
- **A2-2 그룹/페이지 레이턴시 완료 (2026-06-06, subagent-driven 8 task + 영문 제목 fold-in + docs)**: 각 `parallel` 노드가 자기 페이지-로드 레이턴시 분포(run-total `count`+`p50/p95/p99/max_ms` = 동시 분기 블록 wall-clock ≈ max-of-branches)를 리포트에 emit. loop 7-1 / if 9d breakdown 파이프라인을 **HDR(카운트 아님)** 로 재사용 — 엔진 `Step::Parallel` arm이 `join_all`을 `Instant`로 재 `Aggregator.group_hists`에 **clean-flow(`!aborted && !deadline_hit`)일 때만** 기록 → `MetricFlush.group_stats`(4 flush 드레인 + 3 send-guard, open-final 무가드) → proto `GroupStat`/`MetricBatch.group_stats=7` → 워커 HDR 직렬화 → controller `run_group_metrics`(migration **0010**, **append-only**: HDR는 SQL merge 불가라 delta 행 공존+read-merge, PK 없음, 멱등성은 단일 bidi 스트림 배치 전달-once가 보장) → `build_report` 6번째 param `groups`가 step_id별 `merge_into`한 **별도 `group_acc`**(summary/overall/RPS/per_step/windows **절대 미접촉**=이중카운트 방지) → 최상위 `ReportJson.group_latency: Vec<GroupLatency>`(`#[serde(default)]`) → UI Zod `.optional()`+`GroupLatencyTable`(`findStepById` 노드명, StepStatsTable 단위-헤더 컨벤션). **엔진 핫 flat-http·비-parallel run byte-identical, trace 무변경, proto field 7 additive, ADR 신규 불필요(ADR-0033 범위 내 additive)**. task별 2단계 리뷰 + 최종 handicap-reviewer READY-TO-MERGE(wire 1:1) + e2e `parallel_group_latency_report_e2e_smoke` + 라이브(200ms×2분기: 페이지 p50≈206ms=max not sum·summary.count=group.count×2=비오염·`ReportSchema.parse` 통과). 잔여: per-branch bottleneck·초단위 시계열·페이지 성공/오류 분할·loop/if 컨테이너 확장·중첩 parallel.
- **✅ per-branch breakdown 완료 (2026-06-12, subagent-driven 5 task + 리뷰 fold-in 5건)**: 각 parallel 분기별 레이턴시 분포(count+p50/p95/p99/max_ms)를 페이지 행 아래 `GroupLatency.branches`로 중첩 — "어느 동시 호출이 병목인가" 가시화. 설계 질문 해소: ① 기존 `group_stats`/`run_group_metrics`에 `branch` 차원 1개 추가(새 파이프라인 0 — 드레인/guard/proto 메시지/테이블 신설 없음) ② 라벨 = `branch.name`(UI 유니크 gate와 동일 소스) ③ group_stats HDR 형태 그대로. proto `GroupStat.branch=4` / migration **0014**(Rust-guarded ALTER `branch TEXT NOT NULL DEFAULT ''` — 기존 행 페이지 백필) / `build_report` `group_acc` `(step_id,branch)` 키 / UI `BranchLatencySchema`+↳ sub-행. 분기 행 count == 페이지 행 count(같은 clean-block 게이트 단일 락 일괄 기록). 페이지 행 와이어·비-parallel run byte-identical. spec/plan `2026-06-07-parallel-per-branch-latency*`. handicap-reviewer READY-TO-MERGE + 라이브(slow 200ms/fast 2분기: slow p50=210·fast p50=1·페이지 211≈max·summary 비오염·UI 콘솔 0). **잔여(연기)**: ④ 그대로 — per-branch phase(TTFB/다운로드) 3차원·분기 성공/오류 분할·초단위 시계열·중첩 parallel·loop/if 컨테이너 내 parallel·open-loop 상호작용 정밀 검증. ~~**신규 연기(handicap-reviewer Minor)**: 엔진/컨트롤러측 분기명 검증 부재~~ — **✅ 해소 (2026-06-12, 단독 fix)**: 시나리오 create/update 게이트(`api/scenarios.rs::validate_parallel_branch_names`)가 빈 분기명·중복 분기명을 400으로 거부 — `name: ""` 분기의 **페이지 행(branch="") alias 오염**·중복명 silent merge 차단. UI Zod와 의미론 1:1(min(1) no-trim + 정확일치 유니크 — UI-생성 시나리오가 거절되는 비대칭 없음), 엔진은 lenient 유지(자유 중첩 대비 재귀 walk, loop/if 내부 parallel도 검증). 엔진·워커·proto·migration 무변경, 통합 테스트 5개(`scenario_branch_validation_test.rs`). 기존 저장분은 forward-only(백필 검증 없음).
- **spec 근거**: §4.5 "… loop, conditional, **parallel** …".
- **성격**: control-flow 노드(동시 분기). 단일 VU 안에서 여러 스텝/분기를 **동시** 실행 후 join.
- **왜 지금 (2026-06-05 사용자 확정)**: 테스트 대상이 **하이브리드 앱 + 웹뷰**라 프론트엔드 부하(브라우저가 페이지 로드 시 `/api/user`·`/api/feed`·`/api/notifications` 등을 **동시 fan-out**)가 주력이다. 이걸 순차로 모델링하면 백엔드가 보는 **동시성 버스트·커넥션 재사용·per-session 경합**이 전부 틀어진다 — 그리고 이건 **VU 수를 늘려도 대체 불가**다. per-VU 병렬 ≠ VU 스케일인 이유: ① 같은 세션/토큰/쿠키 jar(ADR-0018)·HTTP/2 커넥션을 공유하는 **상관된 요청 묶음**의 버스트, ② per-session rate-limit·HTTP/2 멀티플렉싱·세션 캐시는 "공유 세션 fan-out"으로만 재현(600 VU = 600 세션이라 못 만듦). JMeter Parallel Controller / LoadRunner concurrent block의 관용구(ADR-0001 대체 목표와 연결).
- **도출된 우선순위 (load fidelity → reporting depth)**: 생성 부하가 production 트래픽 모양과 다르면 더 깊은 리포트는 "엉뚱한 걸 정밀 측정"이다. **부하 충실도가 먼저.** 순서: **(1) A2 Parallel** ✅ (부하 모양 자체를 고침) → **(2) 그룹/페이지 레이턴시** ✅ (동시 호출의 max = 웹뷰 페이지 로드 KPI; **로드맵 미등록 신규 항목** — A4/B7과 별개) → **(3) B7-C 연결-단계 분해 ✅ 완료 (2026-06-07, TTFB+다운로드 phase, DNS/TCP/TLS 연기)**. B7-C를 먼저 안 했던 이유: hot-path 계측이라 20k RPS 부하생성기 **처리량 회귀 위험** + reqwest/hyper 연결 타이밍 노출이 침습적. 실제 구현에선 executor 측정/runner 게이트 분리(off=byte-identical)로 처리량 회귀 0 확인.
- **standalone 가치 caveat**: A2만 끝낸 시점엔 per-endpoint p95 + run 전체 레이턴시 분포(B7-D, 머지됨)는 보이나 "페이지 로드 = max"는 (2)단계 전까지 직접 안 나온다. 그래도 (1)의 핵심 가치(올바른 백엔드 동시성/버스트 + per-endpoint 메트릭)는 독립 성립.
- **착수 시 brainstorming에서 풀 설계 질문**: VU 실행 모델(ADR-0016, tokio task per VU)과의 상호작용 — ① 분기 **join 의미**(전부 완료 대기 vs first-fail), ② **에러 전파**(한 분기 실패가 VU/노드를 죽이나, lenient하나 — if의 lenient 평가와 비교), ③ **메트릭 집계**(per-step×ts 윈도는 락이 step별이라 서로 다른 step_id 동시 기록은 안전; 같은 step_id를 두 분기가 동시에 치면? — Aggregator 락 경합 재검토), ④ **cookie jar 공유**(VU 단일 jar를 동시 분기가 공유 = 웹뷰 동작과 일치하나 동시 Set-Cookie 경합 의미), ⑤ **중첩 규칙**(loop/if와의 상호 1레벨 중첩 — 9c two-tier Zod 패턴 재사용), ⑥ data-binding/think_time과의 상호작용.
- **참고**: ADR-0020(control-flow loop 패턴), ADR-0023(conditional — two-tier 중첩·lenient 평가), ADR-0016(VU 모델), ADR-0018(VU별 jar). spec §2 모델 노트 "loop/conditional/parallel 노드는 `type` + 내부 `do:` 중첩, 캔버스 컨테이너 시각화".

### A3. 멀티 워커 fan-out (계획된 분산 실행) — **✅ 완료 (A3a+A3b+A3c 머지 완료 — 영역 A3 완결)**
- **spec 근거**: §4.5 "다중 워커 자동 스케일링 (워커는 1대 고정)". spec §1 "OUT — 명시적으로 후속" 3단계 항목(`...mvp1-design.md:127`).
- **성격**: 스케일아웃. §4.3 성능 목표(5,000 RPS)는 단일 워커로 이미 ~20,000 RPS 달성(Slice 6 baseline)이라, 이건 처리량보다 **분산 실행/조정** 슬라이스에 가깝다.
- **결정**: "자동 스케일" = **반응형 HPA 가 아니라 계획된 fan-out**(run 시작 시 N=ceil(총VU÷capacity) 고정, 워커 mid-run 합류/이탈 없음 — 부하 생성기는 "정해진 VU 안정 생성"이 목적). spec `docs/superpowers/specs/2026-06-01-multi-worker-fanout-design.md`, ADR-0027.
- **분할(spec §9)**: **A3a**(컨트롤러 per-run 멀티워커 상태머신 + proto RunAssignment shard 필드 + 엔진 글로벌 vu_id + subprocess N-spawn) ✅ **머지 완료(2026-06-02, subagent-driven 8 task; 코드리뷰가 잡은 완료-집계 race 1건 수정 — terminal 워커 entry 보존)** → **A3b**(메트릭 머지: run_metrics PK 에 worker_id, 워커별 행+읽기 시점 HDR merge, migration 0008) ✅ **머지 완료(2026-06-02, subagent-driven 6 task; 읽기 3 사이트 summary/windows_with_hdr/build_report 가 (ts,step)로 HDR merge+count SUM, loop/if 는 증분 누적이라 무변경)** → **A3c**(K8s Indexed Job parallelism=completions=N + dispatcher cleanup 배선 + Helm `worker.capacityVus`) ✅ **머지 완료(2026-06-02, subagent-driven 6 task; `build_job_spec` 가 worker_count→Indexed Job + worker-id 라벨/arg 제거(Pod 가 JOB_COMPLETION_INDEX 로 파생), `CoordinatorState` 가 `Arc<OnceLock<SharedDispatcher>>` 로 finalize 5 사이트서 cleanup 호출, Guaranteed QoS(req==limit 250m/256Mi) + soft topology/anti-affinity, e2e-kind N=2 단언(완료 시 Job 삭제라 run 진행 중 Job 형상 관측); per-deploy 워커 resource Helm values 는 §B2'' 연기)**. A3a+A3b 한 세트 출하(A3a 단독은 메트릭 keep-first 손실).
- **잠금 해제**: 8c에서 `unique` 바인딩 정책을 "멀티-워커 전역 커서 필요"로 거부했는데(아래 §B1), 이 인프라가 깔린 뒤 **후속** 하위 슬라이스에서 컨트롤러 중앙 커서로 푼다(spec §6.4 설계 스텁 — A3 본체는 unique 여전히 거부, fail-fast 만).
- **참고**: ADR-0019 (워커 dispatcher 추상화 — subprocess/K8s Job), ADR-0010 (gRPC pull/등록 모델). 컨트롤러의 run→워커 분배(VU split, 메트릭 머지)가 핵심.
- **반응형 HPA (연기)**: CPU/메트릭 기반 run 중 스케일은 부하 생성기엔 부자연(VU 수 흔들리면 측정 흔들림). 되살리려면 동적 멤버십·VU 재분배·부분 메트릭 머지 설계부터. → 별도 후보.

### A4. LoadRunner급 리포트 깊이
- **spec 근거**: §4.5 "LoadRunner급 리포트 깊이 (run 비교·SLA·트랜잭션 분해)". §3.3 "MVP 리포트 OUT" 의 트랜잭션 분해·워터폴·히스토그램·CSV/Excel export(`...mvp1-design.md:446`).
- **성격**: controller(리포트 빌드) + UI 중심. 엔진/워커 변경 적음(트랜잭션 시간 분해 DNS/TCP/TLS/TTFB는 엔진 계측 필요 — 그 하위 항목만 엔진 손댐).
- **참고**: ADR-0017 (MVP 리포트 스코프 — "run간 비교·SLA는 후속"을 명시). run 간 비교 = 다중 run 선택 UI + 델타 뷰, SLA = pass/fail 임계 정의 + 판정.
- **A4a (run-level SLO / pass-fail criteria) ✅ 완료 (2026-06-03, subagent-driven 8 task)**: 종료된 run 리포트에 run-level criteria(p50/p95/p99·error_rate·min_rps) pass/fail verdict. profile_json 스냅샷 저장(무마이그레이션) + completed-only on-demand `build_report` verdict(B2) + `validate_run_config` 검증 + RunDialog SLO 입력(%↔분수, 프리셋 포함) + 리포트 `VerdictPanel`. ADR-0028. spec `docs/superpowers/specs/2026-06-03-a4a-slo-pass-fail-criteria-design.md`, plan `docs/superpowers/plans/2026-06-03-a4a-slo-pass-fail-criteria.md`. A4b(run 비교)·A4c(요약)는 후속.
- **A4b (run 비교 + 리포트 CSV/XLSX export) ✅ 완료 (2026-06-03, subagent-driven 15 task 2-phase)**: 같은 시나리오 종료 run 2–5개 화면 비교(baseline 대비 Δ) + 단일/비교 CSV·XLSX export. **하이브리드** — 비교는 클라(기존 `/report` N-fetch + `compareReports` 순수 변환 → `CompareMatrix`), export는 컨트롤러(`build_report` 재사용 + `csv`/`rust_xlsxwriter`, 4 라우트). same-scenario terminal-only, N≤5 화면·>5 export(상한 50). 델타 공식은 spec §4.3 권위 정의 + `testdata/compare_golden.json` 골든 fixture로 TS↔Rust 패리티 교차검증. `build_report_for_run` 공유 헬퍼, `downloadFile`(fetch→blob, 4xx 배너). **엔진·워커·proto·마이그레이션 무변경.** ADR-0030. spec `docs/superpowers/specs/2026-06-03-run-comparison-report-export-design.md`, plan `docs/superpowers/plans/2026-06-03-run-comparison-report-export.md`. 연기 → §B7.
- **A4c (actionable 리포트 요약) ✅ 완료 (2026-06-03, subagent-driven 10 task, master `6c77401`→`3530c30`)**: 종료 run 리포트 최상단에 결정론적 인사이트 패널. 백엔드 `build_report`가 신규 순수 `insights.rs::derive_insights` → `ReportJson.insights: Vec<Insight>`(구조화 emit, UI 한국어 렌더). v1 6종: `slowest_step`·`error_hotspot`·`no_request_step`(시나리오 walk, 무조건-도달 스텝만, if 분기 제외)·`slo_failure`/`slo_pass`·`status_class`(4xx/5xx, status-0 분모 제외)·`status_temporal`(후반 5xx). 신호 있을 때만 emit(패딩 없음, run_health 의도적 미채택), `order_rank` stable-sort. 단일-run XLSX **Insights 시트** 추가, CSV/compare/엔진/워커/proto/마이그레이션 무변경. **`#[serde(default)]`**(골든 fixture 호환) + strict `ReportSchema`에 `.optional()` 키 추가. 최종 handicap-reviewer READY-TO-MERGE(wire 1:1), 게이트 green(controller 105 pass+clippy / UI 393 pass+tsc-b). spec `docs/superpowers/specs/2026-06-03-a4c-actionable-report-summary-design.md`, plan `docs/superpowers/plans/2026-06-03-a4c-actionable-report-summary.md`(10 task). **연기**: baseline 회귀(A4b 연계)·status-class verdict 기준(A4a')·CSV/compare 인사이트 → §B7. **→ A4 영역(A4a+A4b+A4c) 완결.**

### A5. Run 설정 재사용 — Run 프리셋 + Retry (영역 A) — **✅ 완료 (A1 + A2 머지, ADR-0024)**
- **성격**: QoL/UX 슬라이스(§4.5 메뉴 밖, 사용자 요청 발). run 을 한 번 돌린 뒤 같은 설정을 매번 손으로 재입력하는 통증 해소.
- **spec**: `docs/superpowers/specs/2026-05-30-run-presets-retry-design.md` (brainstorming 2026-05-30 + spec-plan-review 반영 2026-05-31).
- **요지**: 별도 `run_presets` 테이블(scenario-scoped, UNIQUE(scenario_id,name), migration 0005) + REST CRUD + RunDialog "프리셋 불러오기/저장" + 과거 run "다시 실행"(prefill) / "즉시 재실행". retry 는 `GET /api/runs/{id}` 가 이미 노출하는 profile+env 재사용 → 신규 저장 0. 시나리오 변경 시 경고 배지(run retry 한정; 프리셋은 라이브 추종).
- **두 하위 슬라이스로 분할**(spec §8): **A1 = Retry**(DB 변경 0, RunDialog prefill 이음새 구축 — 현재 RunDialog 는 prefill prop 이 없어 리팩터 필요), **A2 = 프리셋 CRUD**(검증 게이트 `validate_run_config` 추출 + 데이터셋 DELETE soft 가드 확장 포함).
- **다음 단계**: A1 부터 writing-plans 로 구현 계획 작성.

### A6. 글로벌 변수 = 환경(Environments) (영역 B) — **✅ 완료 (B-1 + B-2 머지)**
- **성격**: BASE_URL 등 자주 쓰는 변수를 전역 등록 → 아무 시나리오에서나 골라 주입. 영역 A 의 자매 기능(같은 "재입력 통증" 원천).
- **결정·진행**: spec `docs/superpowers/specs/2026-05-31-global-variables-environments-design.md`, ADR-0025. 2분할 모두 머지 — **B-1(환경 리소스 + 관리 UI: migration 0007 `environments` 테이블 + CRUD REST + `EnvironmentsPage` + 클라/hooks)**, **B-2(RunDialog 환경 오버레이: `resolveEnv` 순수 병합 + standalone `<EnvironmentPicker>` — env 미선택 = pre-B2 byte-identical, `POST /api/runs` 무변경)**.
- **후속(연기)**: 민감값 마스킹(roadmap B1), `{{var}}` 흐름변수 전역 등록, 시나리오 에디터 환경 선택 test-run(spec §7 — `resolveEnv`/`<EnvironmentPicker>` 재사용 이음새 준비됨).
- **해소된 설계 질문**: 스코프 = `${ENV}` 네임스페이스만(`{{var}}` 흐름변수는 범위 밖). 주입 시점 = 클라이언트 병합 스냅샷(`POST /api/runs` 무변경, 우선순위 환경 < per-run override). 환경 묶음 = named environments(top-level 독립 리소스, scenario_id/FK 없음, 무가드 DELETE).

### A7. Run 스케줄러 (예약/반복 실행) — **✅ 완료 (34a+34b 백엔드 + 34c UI, ADR-0034)**
- **성격**: QoL/운영 자동화 슬라이스(§4.5 메뉴 밖, 운영 요청 발). 종료 run을 매번 손으로 `POST /api/runs` 하지 않고 once(특정 일시)/cron(반복)으로 자동 발사. 반복 발사가 A4a SLO verdict·A4b run 비교·A4c insights와 맞물려 성능 회귀 감시 루프가 됨.
- **결정·진행**: spec `docs/superpowers/specs/2026-06-06-run-scheduler-design.md`, ADR-0034. 3분할 — **34a(백엔드 코어)** ✅ 머지: 순수 트리거 엔진 `schedule/trigger.rs`(`croner`/`chrono-tz`, once/5-field cron 다음 발사 계산·검증, DB/IO 없음) + `api::runs::spawn_run` 발사 코어 추출(REST `create`와 공유, byte-identical). **34b(영속화+루프+REST)** ✅ 머지(2026-06-06, subagent-driven 5 task, task별 2단계 리뷰): migration 0011 `schedules`/`schedule_events` + `store/schedules.rs`(CRUD + `due`/`mark_outcome` + events) + 컨트롤러 내장 `run_scheduler` 루프(`schedule/runner.rs`: due 틱→겹침/missed/error 판정→`spawn_run`, 매 발사 `validate_run_config` 재검증) + `api/schedules.rs` CRUD/preview-next/events REST + main.rs CLI 3종(`--scheduler-tick-seconds`/`--scheduler-timezone`/`--scheduler-disabled`) 배선. **34c(UI)** ✅ 머지(2026-06-07, subagent-driven 9 task, task별 2단계 리뷰 + 최종 handicap-reviewer): `/schedules` 페이지(`EnvironmentsPage` 미러 — 목록/생성·편집 폼/삭제/enable 토글(summary엔 profile·env 없어 get→put)/이벤트 타임라인) + RunDialog `buildProfile`/`buildCriteria`/SLO 그리드를 공유 `profileForm.ts`+`<CriteriaFields>`로 **순수 추출**(payload byte-identical, RunDialog 53 테스트 무수정; RunDialog도 공유 헬퍼 사용=단일 소스) + 5모드 트리거 빌더(once/daily/weekly/interval/advanced, 순수 `triggerCron.ts` 프리셋→5-field cron 컴파일·`describeTrigger`, cron 평가는 서버 `preview-next` 단일 위임) + `TriggerBuilder` 라이브 preview-next(debounce 400ms) + Zod 와이어 스키마(Option `.nullish()` 서버-null) + `ScheduleForm`이 `LoadModelFields`/`DataBindingPanel`(시나리오 변경 reseed-by-key)/`EnvironmentPicker` 재사용. **UI-only(엔진·워커·proto·controller·migration 무변경), ADR 불필요.** 라이브(Playwright): cron `*/1` 발사→run completed→이벤트 타임라인 `fired`+리포트 링크, 콘솔 Zod 0(S-D 갭 차단). (`GET /schedules/{id}`는 상세만, 이벤트는 `/events` 전용 — inline-events 미재도입 준수.)
- **의미론(ADR-0034)**: 컨트롤러 내장 주기 `tokio::spawn` 루프(외부 K8s CronJob·사이드카 기각 — always-on 조정자라 새 인프라 0), once|5-field cron(`validate_trigger` 필드수==5 강제 — croner는 6·7-field도 파싱), 단일 IANA TZ(`AppState.scheduler_tz`, `--scheduler-timezone` 기본 Asia/Seoul, main.rs 1곳 파싱→루프+REST 공유), 겹침=skip(`skipped_overlap`) / cron missed=버리고 전진(grace 300s) / once=grace 무관 1회 발사 후 비활성, 모든 주목 이벤트 append-only `schedule_events`(알림 레이어 이음새). **엔진·워커·proto 무변경, 기존 `AppState {…}` 사이트에 `scheduler_tz`만 추가해 byte-identical, migration 0011 추가.**
- **연기**: 알림(이메일/슬랙/웹훅, 이음새=`schedule_events`)·per-schedule TZ(+DST 정책)·catch-up 모드·`runs.schedule_id` 역링크·이벤트 보존정책·프리셋에서 시드·멀티 컨트롤러(leader election)·스케줄 폼 think-time(pacing) UI·편집 시 cron→프리셋 역파싱(현재 advanced raw 로드).

### A8. 영역 U — UX 초보자 친화 재설계 — **✅ 완결 (U1a+U1b+U2+U3+U5+U4 전부 머지, 2026-06-12)**
- **성격**: UX/QoL 프로그램(§4.5 메뉴 밖, 사용자 요청 발 2026-06-11). 초보 QA(HTTP 기본 지식 가정)가 문서 없이 시나리오 생성→실행→결과 판단까지 자력 완수, 고급 사용자 주 동선 클릭 수 불변. **전 슬라이스 UI-only**(엔진·proto·migration 무변경).
- **spec**: `docs/superpowers/specs/2026-06-11-ux-beginner-friendly-redesign-design.md` (brainstorming 섹션별 승인 + spec-plan-reviewer 2회). 결정: 점진 노출(C) 기본 + 홈만 가이드 레이어(위저드 미채택, 부하 크기 프리셋 chips는 RunDialog로 흡수), 한국어 통일 + 신규 문구 `ko.ts` 카탈로그(ADR-0035), `RequestModel.url` min(1) 완화(U3 선행).
- **분할(spec §8)**: **U1a**(카탈로그+HelpTip+용어사전+Summary 최소 소비처) ✅ **머지 완료 (2026-06-11**, subagent-driven 5 task + handicap-reviewer + 라이브 검증, plan `docs/superpowers/plans/2026-06-11-ux-u1a-catalog-helptip.md`**)** → **U1b**(RunDialog 3그룹 재구성+chips+한국어화+막힘 사유) ✅ **머지 완료 (2026-06-11**, subagent-driven 5 task + handicap-reviewer must-fix 2 반영 + 라이브 3 run PASS, plan `docs/superpowers/plans/2026-06-11-ux-u1b-rundialog-restructure.md`**)** → **U2**(홈 가이드 카드+빈 상태 5종+네비+breadcrumb 5페이지) ✅ **머지 완료 (2026-06-11**, subagent-driven 5 task + handicap-reviewer 8 불변식 READY + polish 1커밋 + 라이브 Playwright 온보딩 풀사이클 PASS, plan `docs/superpowers/plans/2026-06-11-ux-u2-home-onboarding.md`**)** → **U3**(템플릿 갤러리 4종+에디터 진입 장벽: url 완화·캔버스/패널 재라벨·⚠ 배지·변수 치트시트) ✅ **머지 완료 (2026-06-11**, subagent-driven 6 task + handicap-reviewer 9 불변식 READY + 라이브 Playwright PASS(갤러리→시드→run 25k req·콘솔 0), plan `docs/superpowers/plans/2026-06-11-ux-u3-template-gallery-editor.md`**)** → **U5**(리포트 해석: §7 전체 — ReportHeadline 쉬운 요약+verdict 콜아웃/sloHint·Summary 카드+표 3종 헤더 한국어화+p50/p95/p99 HelpTip·인사이트 6종 "다음 행동"·useNow 기반 stalled 배너/run 목록 경과·VerdictBadge FAIL 클릭 popover+`usePopover` 추출) ✅ **머지 완료 (2026-06-12**, subagent-driven 5 task + task별 2단계 리뷰 fold-in 3 + handicap-reviewer READY-TO-MERGE(should-fix 1 = metrics 미수신 중 배너 플래시 가드 fold-in) + 라이브 Playwright PASS(합격/불합격 콜아웃·3변형 문장·FAIL popover 목록/상세·HelpTip·인사이트 행동 줄·콘솔 0), plan `docs/superpowers/plans/2026-06-12-ux-u5-report-interpretation.md`**)** → **U4**(검증 배너+test-run 승격: §5.4–5.5 — 에디터 상단 시나리오 문제 요약 배너 2계층(모델-가용=빈 URL·host-less `/` 스텝, 클릭 시 스텝 선택+캔버스 탭 / 게이트 에러=Zod·YAML 원문 한국어 매핑+원문 fallback, 스텝 선택 비활성+"YAML 탭에서 확인", yamlError 캔버스 탭 무표시 갭 해소) + `TestRunSection` forwardRef `runNow()` 핸들 + 두 페이지 헤더 "미리 1회 실행"+HelpTip) ✅ **머지 완료 (2026-06-12**, subagent-driven 4 task + task별 2단계 리뷰 + 리뷰 fold-in 4(공백/프로토콜-상대 URL 핀·게이트 행 비클릭 핀·pending 스크롤 핀·실-Zod 드리프트 가드) + handicap-reviewer READY-TO-MERGE + 라이브 Playwright PASS(배너 모델/게이트 2계층·클릭-선택·YAML 탭 유도·미리 1회 실행 두 페이지 trace 200·HelpTip popover·콘솔 0), plan `docs/superpowers/plans/2026-06-12-ux-u4-validation-banner-testrun.md`**)** — **영역 U(ADR-0035) 완결.**
- **U4 연기 항목**: 게이트 매핑 미커버 문구는 영어 원문 fallback(spec 허용) — ~~Zod discriminator-mismatch(`Invalid discriminator value…`)·`.strict()` `Unrecognized key(s)`·컨테이너 `min(1)` 문구류가 다음 매핑 후보~~ **✅ gate-error-ko 해소(2026-06-19 — discriminator/enum/unrecognized-key/string·container `min(1)`/`repeat>=1` 클래스를 `ko.editor.gate*`로 매핑 + 실-Zod 3.25.76 드리프트 가드, model.ts 0-diff)**. 다음 매핑 후보 = 제네릭 숫자 범위(`Number must be ≥/≤ N` — assert code·timeout·think_time)·조건 `z.union` 실패(`Invalid input`, 노출 구체값 없음)·think-time refine·ULID regex·JSON cast 에러(전부 영어 fallback 유지, spec §7) · ~~스킴-없는 호스트 URL(`example.com/path`)은 미검출(`/`-prefix만 — 엔진은 동일 fail-fast지만 배너 침묵)~~ **✅ editor-ux-polish 해소(2026-06-19, `problemUrlNeedsScheme` 일반화)** · 게이트 항목 갱신은 Monaco 타이핑 중이 아니라 debounce commit 시점(기존 store 의미론) · 게이트 "YAML 탭에서 확인" 버튼이 YAML 탭에서도 표시(무해 no-op) · 배너 SR 읽기 순서(액션 버튼이 intro보다 먼저) polish · TestRunSection 잔존 영어("Test run"/"Max requests", 소급 추출 비목표 — ko.common 일괄 시) · ~~헤더 "미리 1회 실행" pending 피드백 없음(섹션 버튼만 "Running…", isPending 가드로 중복 발사는 차단)~~ **✅ editor-ux-polish 해소(2026-06-19 — 헤더 버튼 자체 제거, test-run 단일 섹션 통합)**.(Report/Summary/Steps/Page load latency/Branch decisions/Latency/Status codes — §7.2 문언은 "표 헤더"라 섹션 제목은 범위 밖)·TimeSeriesChart 제목·다운로드 버튼·RunDetailPage 라이브 카드·run 목록 컬럼 헤더(전부 ko.common 도입 시 일괄) · B7-C 기존 "다운로드 p50 ms" 헤더·BranchStats `(미매치)` 인라인 카피의 카탈로그 이전(소급 추출 비목표라 잔존 — 표가 half-catalog 상태) · HelpTip `label="p50 설명"` 리터럴 → `ko.report.helpLabel(term)` 단일화 후보 · FAIL popover가 run 상세 h2 안에서 열리는 동안 미달 기준 행이 heading accname에 일시 합류(배지 형제 배치/portal 후속) · headline `errPct` 0.05% 미만 "에러 0.0%" 표기("<0.1%" floor 후보) · stalled 배너 라이브 미재현(dispatch fail-fast가 즉시 failed로 만들어 register-후-사망만 해당 — RTL 4케이스로 갈음) · HelpTip/usePopover Modal-내 ESC 레이어링(usePopover 주석 문서화, 소비처 전부 Modal 밖).
- **U2 연기 항목**: 홈 `Loading…`/`Failed to load:` 영어 잔존(소급 추출 비목표 — ko.common 도입 시 일괄) · UploadPanel 가시 한국어 제목(데이터셋 빈 상태 "위 업로드 패널" 위치 참조의 anchor — 데이터셋 표면 한국어화 시) · **U3 출하 시 `ko.empty.scenarios` 템플릿 문구 갱신 책임**(U2는 갤러리 부재라 의도적 무언급) · `ko.breadcrumb.runs`↔`ko.pages.runsTitle` 동일 문자열 별도 키(역할 분리 변호 가능, en.ts 도입 시 재검). 시나리오 전부 삭제 시 가이드 카드 재등장(②③ ✓ 유지)은 의도된 동작(영구 숨김은 dismiss만).
- **U3 연기 항목**: Inspector 나머지 필드 라벨(Name/Method/Headers/Body/Timeout/Think 등)·EditPage chrome·`Remove variable ${key}` aria·EnvironmentsPage "No variables"(ko.common 후속 일괄) · ~~VariablesPanel `?? {}` getSnapshot 경고(기존 잠복)~~ — **✅ 해소(2026-06-12, 단독 fix `0a02c49`)**: 같은 클래스 CanvasView·Inspector×2 `?? []`까지 모듈 안정 상수(`EMPTY_VARS`/`EMPTY_STEPS`)로 일괄 교체 — model=null 단독 마운트는 경고가 아니라 무한 리렌더 크래시였음(핀 테스트 2건, ui/CLAUDE.md Zustand 함정 기록) · 동일-화면 VarCheatSheet aria-label 2개(풀페이지 테스트 시 getAllByRole) · blank 템플릿 `name: "Untitled"`(store STARTER 물리 단일화 시 동반) · ~~**U4 인계**: ⚠ 배지 title-only·host-less `/` 시드 URL 검출 검토~~ — **✅ U4에서 해소(2026-06-12)**: 검증 배너가 접근 가능 열거(role=status+클릭 행) 담당, host-less `/` 검출 포함(`problemHostlessUrl`).
- **U1b 연기 항목**: `ko.loadModel.sizePresets`의 동작 숫자(vus/duration) 분리(en.ts 도입 시 카탈로그→loadModel.ts) · DataBindingPanel auto-match의 updater-내 Set mutate 리팩터(StrictMode 안전하나 안티패턴) · 인라인 에러 p ↔ 사유 블록 문구 카탈로그 통일 · ScheduleForm 주석 표현 정밀화 · ko.runDialog 네임스페이스의 ScheduleForm 차용(공용 common 분리 후보).
- **U1a 연기 항목**: HelpTip Modal-내 ESC 레이어링(U5 VerdictBadge popover 전 설계 필요, `ui/CLAUDE.md` 기록) · 224px 미만 초협소 뷰포트 좌단 오버플로(실질 무관) · 키보드 전용 multi-tip 동시 오픈(무해) · HelpTip focusout 닫힘 polish.

### A9. 부하 생성기 용량 추천 / 포화 인사이트 — **v1 + 사이징 권장 + 닫힌 루프 create-time VU 사이징 헬퍼 + 열린 루프 create-time 슬롯 사이징 헬퍼 + open+curve 슬롯 힌트 + worker_count create-time 사이징 헬퍼 + XLSX Insights 사이징 3열 + mean 지연 프록시 전면 업그레이드(C) 완료 (2026-06-15)**
- **✅ v1 완료 (2026-06-14, ADR-0028 범위 내·신규 ADR 불필요)**: open-loop run-total `dropped > 0`이면 `derive_insights`가 `load_gen_saturated` 인사이트 1종을 emit — `value` = 관측 천장(= **peak per-second throughput** = 초별 윈도 count 합의 최대; whole-run `summary.rps` 평균은 ramp에서 천장을 과소평가해 미사용, 윈도 없으면 `summary.rps.round()` 폴백), `count` = `dropped`, `severity` = warning. `order_rank` rank 3(5xx 다음, no_request_step 앞 — 이하 +1 시프트로 기존 kind 상대순서 보존) + UI `InsightPanel` 헤드라인 + `ko.insightActions` 다음 행동(초보자 친화: "테스트 도구"/"대상 서버", latency/max_in_flight 인라인 병기). **`dropped == 0`이면 리포트 byte-identical**(closed-loop·비포화 open-loop 미emit), proto/migration/engine/worker/UI-Zod 무변경. 라이브 PASS(dropped=24031·value=1152=peak≠avg773·count=24031·closed-loop 미emit·실브라우저 앰버 카드 2줄·콘솔 Zod 0). spec `2026-06-14-load-gen-saturation-insight-design.md`, plan 동명, build-log 참조. **연기(아래 §B·spec §8)**: ~~Little's Law 사이징 권장~~(✅ 아래 사이징 권장으로 완료)·open-loop misconfig 경고·per-window dropped 정밀 핀포인트·"achieved vs 고정 target 부족분" arm·원인(부하기 vs SUT) 자동 귀속(v1은 사용자 위임).
- **✅ 사이징 권장 완료 (2026-06-14, ADR-0028 범위 내·신규 ADR 불필요)**: 위 v1을 enrich — `dropped > 0`이면 Little's Law(`required = ceil(target_rps × p50_sec)`, floor ≥1)로 설정 `max_in_flight`와 비교: `max_in_flight < required` → `cause="slots"` + `recommended`=required(권장 max_in_flight 숫자), `≥` → `cause="capacity"`(올려도 무익 = 워커 CPU/SUT 한계), `p50==0`·profile 부재 → 폴백(A9 일반 문구). `Insight`에 optional 2필드(`recommended: f64`/`cause: String`, `skip_serializing_if`) 가산 + UI Zod `.optional()` + `InsightPanel.actionFor` cause 분기 + `ko.saturation.slots/capacity`. 유효목표 = `target_rps` or stages-peak(report.rs 주입), 지연 프록시 = `summary.p50_ms`. **proto/migration/engine/worker/골든 fixture 무변경, `dropped==0`·폴백이면 byte-identical**(유일 와이어 변화 = cause∈{slots,capacity}). subagent-driven 2 task + handicap-reviewer READY-TO-MERGE + 라이브 PASS(slots: p50=53→required=106=recommended·count=8181·실브라우저 "최소 ~106로 올려" 앰버 2줄·콘솔 Zod 0 / capacity: max_in_flight 8000≥required 2300→cause=capacity·recommended 부재 / closed-loop 미emit). spec/plan `2026-06-14-capacity-sizing-recommendation*`. **알려진 v1 한계**(spec §2.3): `p50<mean`이면 capacity-bound run이 slots로 오분류 가능 → slots 문구가 "에러·지연 함께 높으면 SUT" 교차판단으로 흡수. 연기 → ~~closed-loop `vus` 권장~~(✅ 아래 create-time 헬퍼)·per-window dropped·~~create-time 사이징 힌트~~(✅ 아래 create-time 헬퍼)·open-loop misconfig 경고·~~XLSX recommended/cause 열~~(✅ 2026-06-15 "XLSX Insights 사이징 3열"로 완료 — `recommended`/`cause`/`recommended_workers` 3열)·~~mean 프록시 전면 일관 업그레이드(C)~~(✅ **2026-06-15 완료** — 위 ✅ 불릿: 양쪽 p50→mean·parity 보존·`mean_ms` 노출·reviewer 3라운드 APPROVE).
- **✅ 닫힌 루프 create-time VU 사이징 헬퍼 완료 (2026-06-14, ADR 불필요·순수 UI 슬라이스)**: A9 사후 인사이트의 **거울상** — closed-loop run을 *돌리기 전에* RunDialog 닫힌+고정(균등 VU)에서 "목표 RPS → 권장 VU"를 미리 답한다(A9 `dropped` 트리거는 closed-loop엔 구조적으로 안 떠 사후 권장 불가 → create-time UI가 유일 표면). Little's Law가 선형 스케일로 약분(`targetVus = priorVus × targetRps/priorRps`). 처리량 3계층(신뢰도순) = 최근 종료 closed run 앵커(`summary.rps/profile.vus`, `vus>0`이 open-loop·VU곡선 둘 다 제외)→test-run 1회 측정(trace `R/total_ms`, `truncated`면 부분패스라 거부)→사용자 추정 지연. 측정은 무부하 단발이라 권장값=**하한**("최소 ~N개부터"). 순수 UI 가산: `sizing.ts`(`recommendVus`·`pickLatestClosedRun`) + 자족 `VuSizingHelper`(co-located `usePriorClosedRunAnchor`·비동기 1회 시드 `touchedRef`/`seededRef` race 가드) + `LoadModelFields` optional prop 4종(`onApplyVus` 있을 때만 렌더 → **RunDialog만 전달 = 공유 ScheduleForm엔 헬퍼 부재**) + `ko.sizing`. **엔진·워커·proto·controller·migration·Zod 와이어 무변경, run 페이로드 byte-identical**(머지 diff ui/ 한정). subagent-driven 4 task + handicap-reviewer READY-TO-MERGE + 라이브 PASS(앵커 vus5→rps90.9 프리필91·권장6→목표182→11·적용→VU칸11 / fresh 측정 "요청 1개·57ms"→권장6 / 적용 후 11-VU run completed rps≈199≥182 floor·콘솔 Zod 0). spec/plan `2026-06-14-closed-loop-sizing-helper*`. 연기(spec §10) → VU 곡선 모드 사이징·~~open-loop create-time 슬롯 힌트~~(✅ 아래 열린 루프 슬롯 헬퍼)·open-loop misconfig 경고·측정값 영속화·역방향 라이브 리드아웃·ScheduleForm 헬퍼·reqPerIter 제어흐름 정밀화.
- **✅ 열린 루프 create-time 슬롯(max_in_flight) 사이징 헬퍼 완료 (2026-06-15, ADR 불필요·순수 UI 슬라이스)**: 위 닫힌 루프 VU 헬퍼의 **open-loop 대칭**이자 A9 사후 `load_gen_saturated`의 **create-time 거울상** — RunDialog 열린+고정(open+fixed)에서 run을 *돌리기 전에* "목표 RPS → 권장 `max_in_flight`"를 미리 답한다. 권장 슬롯 = `ceil(target_rps × p50_sec).max(1)` — 사후 인사이트 `insights.rs:224`의 `required`와 **수식·프록시(요청당 p50) 1:1**. open-loop 판별 `pickLatestOpenRun` = `Profile::is_open_loop()` 양성식(`target_rps != null || stages.length>0`; `max_in_flight`는 판별자 아님). 지연 3계층(precedence) = 최근 종료 open-loop run 앵커(`summary.p50_ms`·`p50>0` 가드)→수동 추정→무부하 test-run(`total_ms/R`·`truncated` 거부). 무부하라 권장값=**하한**("최소 ~N부터"). 순수 UI 가산: `sizing.ts`(`recommendSlots`·`pickLatestOpenRun`) + 자족 `SlotSizingHelper`(co-located `usePriorOpenRunAnchor`·`MAX_IN_FLIGHT_CAP=10000` 초과 비차단 경고) + `LoadModelFields` optional `onApplyMaxInFlight`(open+fixed·`onApplyMaxInFlight && sizingScenarioId!==undefined`일 때만 렌더 → **RunDialog만 전달 = 공유 ScheduleForm 부재**, 다른 3모드 미렌더 `it.each` 락인) + `ko.slotSizing`. **엔진·워커·proto·controller·migration·Zod 와이어 무변경, run 페이로드 byte-identical**(머지 diff ui/ 한정). subagent-driven 4 task + handicap-reviewer READY-TO-MERGE + 라이브 PASS(앵커 p50=55→target 200 권장6·1000 권장55·적용→`동시 요청 상한` 55·콘솔 Zod 0 / **수식 parity end-to-end**: max_in_flight=2 과소 run→사후 인사이트 `recommended`=11 = UI 헬퍼 권장과 동일 / 권장 하한 11로 run→drops 830→80·cause slots→capacity 전환=하한 입증). spec/plan `2026-06-14-open-loop-slot-sizing-helper*`. 연기(spec §10) → ~~open+curve 슬롯 힌트~~(✅ 아래 open+curve 헬퍼로 완료)·open-loop misconfig 경고·측정값 영속화·역방향 라이브 리드아웃·ScheduleForm 헬퍼·multi-step 슬롯-홀드.
- **✅ open+curve 슬롯(max_in_flight) 사이징 힌트 완료 (2026-06-15, ADR 불필요·순수 UI 슬라이스)**: 위 open+fixed 슬롯 헬퍼의 **곡선(stages) 확장** — RunDialog 열린+곡선(open+curve)에서 `max_in_flight`는 run 전체 단일값이라 도착률이 가장 높은 단계 기준으로 사이징해야 어느 단계에서도 drop이 없으므로, 폼 stages의 **피크 단계 목표**(`max(stage.target)`)를 유효 목표로 권장 슬롯 = `ceil(peak × p50_sec).max(1)`. 피크 도출 `peakStageTarget(stages)`(`targetRpsValid` 재사용, 유효 정수만 후보·없으면 null)가 사후 `load_gen_saturated`의 곡선 유효목표(`report.rs:616-621` `target_rps.or_else(|| stages.max())`)와 **동일 수식** → 이어지는 `recommendSlots`가 `insights.rs:224` `required`와 **1:1 parity**(open+fixed가 가진 "사전==사후 수식 일치" 불변식이 곡선에서도 성립, prior-run 경로 한정 값동치). 신규 컴포넌트 0 — 기존 `SlotSizingHelper`에 optional `peakBased?: boolean`(기본 false) 1개 추가해 **두 문구만**(formula→`formulaPeak`"최고 단계 목표 N RPS"·needTarget→`needTargetCurve`"단계 목표를 먼저 입력") 곡선 변형, 나머지(앵커·측정·precedence·recommendSlots) 무변경 재사용. `LoadModelFields` open+curve arm에 `peakStr=useMemo(peakStageTarget(stages))` 도출 후 open+fixed와 동형 게이트(`onApplyMaxInFlight && sizingScenarioId!==undefined`)로 렌더(closed+curve는 다른 브랜치라 구조적 미렌더, 슬롯 락인 `it.each`를 open+curve 렌더+closed 2모드 미렌더로 flip), 신규 prop 0(기존 prop 재사용). **엔진·워커·proto·controller·migration·Zod 와이어 무변경, run 페이로드 byte-identical**(머지 diff ui/ 한정). subagent-driven 3 task + handicap-reviewer READY-TO-MERGE + 라이브 PASS(앵커 p50=55→peak 100 권장6·stage 편집 peak 200→권장11·적용→`동시 요청 상한` 11·stage 비우면 `needTargetCurve`·콘솔 Zod 0 / **수식 parity end-to-end**: 곡선 run peak 200·max_in_flight=3 과소→drops 504→사후 인사이트 `cause=slots`·`recommended`=11 = `ceil(200×55/1000)` = UI 헬퍼 권장과 동일). spec/plan `2026-06-15-open-curve-slot-sizing-hint*`. 연기(spec §10) → per-stage 슬롯 분해·open-loop misconfig 경고·측정값 영속화·역방향 라이브 리드아웃·ScheduleForm 헬퍼·multi-step 슬롯-홀드.
- **성격**: 리포트/인사이트 곁다리(읽기 경로 only). 출처 = 사용자 질문 2026-06-14 "RPS 10,000 돌리고 싶은데 컴퓨팅이 받쳐줄지 어떻게 아나? 최대 RPS 추천값·필요 리소스는?".
- **문제**: Handicap은 부하 *생성기*지 용량 *플래너*가 아니라, "지속 가능한 최대 RPS"를 자동으로 알려주지 않는다. 사용자는 현재 `dropped`/`summary.rps`를 눈으로 보고 직접 판단해야 함. 운영 가이드(수동 절차·Little's Law·단일워커 함정)는 **`docs/dev/capacity-planning.md`** 로 정리됨 — 이 슬라이스는 그 판단을 도구가 직접 뱉게 만드는 것.
- **착수 메모(인프라 거의 있음, 순수 가산)**: `dropped` 필드(report.rs:27)·`summary.rps`(=달성 RPS, report.rs:470)·`insights.rs`(ADR-0028 A4c `derive_insights`) 가 이미 있다. 추가할 것 = **포화 인사이트 1~2종**:
  - `load_gen_saturated`: `dropped > 0` **또는** `achieved_rps << target_rps`(임계 비율) 이면 "부하 생성기 포화 — 관측된 지속 최대 ≈ N RPS, 이상은 워커 CPU/`max_in_flight` 증설 필요" 플래그. `insights.rs::order_rank`에 kind 추가(현재 8종, dropped 미검사).
  - (선택) **관측 최대 지속 RPS 역산**: stages 램프에서 `dropped`가 0→증가로 꺾이기 직전 윈도의 달성 RPS를 "현재 구성 천장"으로 표면화. 초별 `dropped` 분해가 없으므로(run-total only) per-window dropped 파이프라인이 필요한지 vs run-total + achieved 곡선으로 충분한지가 설계 질문.
- **brainstorming에서 풀 설계 질문**: ① SUT 한계 vs 부하기 한계를 인사이트가 구분 표기하나(전자=latency/error 인사이트 기존, 후자=신규) ② Little's Law 기반 `max_in_flight`/`vus` 권장값 제안까지 갈까(입력=관측 평균 latency) ③ open-loop 단일워커 함정(§A9 본문·capacity-planning.md §4)을 인사이트가 경고하나(큰 `vus`+`target_rps` 동시 지정 시) ④ per-window dropped 메트릭(drain/guard/proto/migration 비용)을 들일지.
- **의존/연관**: ADR-0028(insights 패턴 재사용), ADR-0031(open-loop·`dropped`·단일워커 v1), ADR-0027(멀티워커 — open-loop RPS 미분할). **§B2'' "per-deploy 워커 cpu/mem Helm values"** 와 묶으면 "권장 → 실제 증설"이 한 흐름이 됨.

### A10. RBAC / 기관 도입 보안 하드닝 — **후보 (브레인스토밍 → ADR 선행, 2026-06-14 등록)**
- **성격**: 보안/거버넌스 프로그램(§4.5 "인증·RBAC·사용자 계정" 승격). 출처 = 사용자 결정(2026-06-14) — 배포 타깃이 사내 단일 테넌트가 아니라 **공공기관**이라, 기존 §4.5의 "사내 단일 테넌트 가정에선 후순위" 전제가 깨진다. 접근통제·감사로그·자격증명 보호가 보통 도입 게이트(보안성 검토)에 들어가므로 "후순위"가 아니라 **도입 전제**로 격상.
- **핵심 아키텍처 분기 (착수 전 ADR로 결정)**: (A) 앱 내 계정/비밀번호 직접 구축(사용자·역할 모델 + 비번 저장 + 전 API 라우트 인가 + UI 게이팅 — 컴플라이언스 표면 큼) vs **(B) 인증은 위임·인가만 인앱**(K8s ingress 앞 SSO/OIDC 또는 oauth2-proxy가 인증, 앱은 forwarded identity 신뢰 + RBAC만 인앱). 공공기관은 대개 정부 SSO/LDAP/행정 IdP가 이미 있어 **(B) 권장**(비번 미보관 = 컴플라이언스·유지보수 유리).
- **묶음(보안 하드닝 트랙)**: §B1 **민감값 마스킹**(env/dataset의 비번·토큰이 로그/리포트/UI/test-run 본문에 평문 노출) + **감사 로그**(누가·무엇을·언제 — run 생성/시나리오 변경/env 조회)와 한 트랙. RBAC의 "누가 이 env를 볼 수 있나"가 마스킹과 만나는 지점.
- **규모**: 작은 슬라이스 아님 = 프로그램(전 라우트 인가 + UI 게이팅 + identity 모델/위임 + 감사). 분해 필요. **코드 전에 전용 brainstorming → ADR**(인증 위임 vs 인앱 계정 결정, §4.5 "후순위" 노트를 supersede)가 선행.
- **참고**: ADR-0001(1차 사용자 = 사내 QA → 공공기관 도입으로 테넌시 가정 재검토), ADR-0006(배포 = upstream K8s → ingress 인증 위임 자연스러움), §4.5 메뉴.

### (메뉴에 있으나 당장 후보 아님)
- WebSocket 노드 (§4.5) — REST 부하 도구의 1차 스코프 밖.
- ~~인증·RBAC·사용자 계정~~ → **§A10으로 승격** (공공기관 배포로 "사내 단일 테넌트" 전제가 무효 — 도입 전제로 격상).
- 라이브 대시보드 (§4.5) — **ADR-0009로 MVP 범위에서 영구 제외**(종료 후 리포트 + APM). 되살리려면 ADR 재검토부터.

---

## B. 슬라이스에서 의도적으로 연기한 자잘한 항목

각 항목은 출처 슬라이스 기준으로 "왜 연기했나 + 어느 슬라이스에서 자연히 풀리나"를 적는다. 어느 슬라이스를 하든 그 슬라이스 plan 작성 시 이 목록을 훑어 관련 항목을 흡수한다.

### B1. Slice 8c (data-driven) 연기 항목
- ~~**단일 바인딩 한계 (한 run = 데이터셋 1개)**~~ — **✅ 완료 (다중 데이터셋 바인딩, 2026-06-16, ADR-0022 구조적 확장)**: 한 run에 N개 독립 바인딩(`Profile.data_binding: Option`→`data_bindings: Vec` + `data_bindings()` 접근자 단일소스). proto field 10 `data_bindings`+`DatasetBatch.binding_index` 가산(field 5 fallback), 엔진 3경로 break-on-first-None 주입, `validate_run_config` 교차검증(변수명중복·MAX_BINDINGS=8), UI 바인딩 카드 목록(RunDialog+ScheduleForm lockstep). migration/ADR 0, 무바인딩 byte-identical. 상세 → `docs/build-log.md`, spec/plan `2026-06-15-multi-dataset-binding*`. 연기(§2 비목표): 행 zip/join·데이터셋 네임스페이스(`{{tracks.name}}`)·cartesian·`on_exhaust:fail` 토글·UI 클라 MAX_BINDINGS 미러.
- ~~**`unique` 바인딩 정책**~~ — **✅ 완료 (2026-06-02)**: 정적 disjoint 슬라이스(`shard_split`) + stop-VU on exhaust 방식. ADR-0022 갱신. 연기: `on_exhaust: fail` opt-in 토글.
- **민감값 마스킹**: 데이터셋 값이 로그/리포트/UI에 노출되지 않게(비밀번호 컬럼 등). 8c는 값 비로깅까지만. → 보안 강화 슬라이스 또는 A4(리포트) 곁다리.
- ~~**JSON 숫자 주입**~~ — **✅ 완료 (2026-06-03, ADR-0029)**: flow `{{var:num}}`/`{{var:bool}}`(+`:str`) 캐스트 토큰으로 JSON body 문자열 leaf를 number/bool로 coerce(순수 단일 토큰 leaf만, leaf 레벨 파싱이라 `template.rs` 무변경, 엄격 실패=`CastFailed`, UI Zod 검증). 연기: `:json`/변수 기반 null·`${env}` 토큰 캐스트·form/raw/URL 캐스트.
- **Helm `datasetMaxRows` 노출**: 8c가 추가한 `--dataset-max-rows` CLI 플래그를 Helm values로. → **A3** 또는 deploy 정리 시.
- **바인딩 throughput 실측 벤치**: `just bench-throughput` 하네스가 `data_binding` profile을 못 구동(8c 이전 작성). 8c는 해석적 no-op 논증만. → 벤치 하네스 개선 + binding 시나리오 추가.

### B2. Slice 8b (데이터셋 리소스) 연기 항목
- **UploadPanel 미리보기 요청 시퀀싱 없음**: 빠르게 옵션 바꾸면 미리보기 응답이 경합할 수 있음(최신 요청만 반영하는 abort/seq 없음). → UI 폴리시 슬라이스.

### B2'. Run 프리셋(영역 A) 연기 항목
- ~~**시나리오 복제**~~ — **✅ 완료 (2026-06-06)**: UI-only 즉시 복제(클라). 유일 `(copy)` 서픽스(`cloneName`) + `renameScenarioYaml`(Document API, 주석 보존) + `useCloneScenario`(기존 createScenario 재사용 + 목록 invalidate) + 목록 행/에디터 헤더 진입점 + 에디터 dirty 시 저장 확인 다이얼로그(저장 후/저장 없이/취소, 저장 실패 시 "저장본으로 복제" 계속 확인). 백엔드/proto/migration/엔진/워커 무변경. spec `docs/superpowers/specs/2026-06-06-scenario-clone-design.md`, plan `docs/superpowers/plans/2026-06-06-scenario-clone.md`. ADR 불필요(additive). 라이브 검증: 목록 복제 (copy)/(copy 2) dedup·화면 유지, 에디터 저장-후-복제 → 원본 v2 저장 + 복제본(copy 3) 저장본 기준 + navigate.

### B2''. A3 (멀티 워커 fan-out) 연기 항목
출처 brainstorming/spec-review(2026-06-01). spec `2026-06-01-multi-worker-fanout-design.md` §11.
- **반응형 HPA**: 위 §A3 "반응형 HPA (연기)" 참조. run 중 동적 스케일.
- **best-effort / degraded 모드 + per-run 토글**: 워커 일부 실패해도 run 지속(나머지 완주 → "부분 완료" 표기). A3 는 **fail-fast** 만(샤드 누락 = 요청 부하 미생성 = 명시적 실패가 안전). 토글(`on_worker_failure: fail|continue`)은 profile 필드 한 줄이지만, "continue" 경로의 기계장치(degraded 상태 enum·per-shard 커버리지 추적·리포트/UI 배지)가 살집이라 별도. profile 이음새만 비워둠. → 보안/리포트 곁다리 또는 단독.
- ~~**운영 상한 관리자 화면**~~ — **✅ 완료 (2026-06-16, 하이브리드 v1)**: 산재한 op-config 상한을 `/settings` 한 화면에 모으고, 컨트롤러 per-request 상한 **6종**(`worker_capacity_vus`·`dataset_max_rows`·open-loop `worker_count` 상한[기존 하드캡 64]·`max_data_bindings`·`max_loop_breakdown_cap`·`max_test_run_requests`)을 **DB 오버라이드 + 인메모리 `SettingsState` 스냅샷**(레지스트리 `static SETTINGS` 단일소스·유효값=override??시드·migration 0017)으로 재배포 없이 런타임 가변. capacity는 coord에서 settings로 단일권위 통일. 엔진 subprocess 상수(`MAX_TRACE_BODY_BYTES` 1 MiB)·스케줄러(`scheduler_tick_seconds`)는 **읽기전용 표시**. 무오버라이드 byte-identical·엔진/워커/proto 무변경. spec/plan `2026-06-16-ops-config-limits-admin*`, build-log 상세. **연기(§7)**: **option-2(엔진 `MAX_TRACE_BODY_BYTES`/`INLINE_PREVIEW_CHARS` worker plumbing → 런타임 가변+단일소스 — 사용자 결정 명시 후속, 엔진/워커/proto/UI빌드를 건드리는 더 큰 슬라이스)**·스케줄러 config(tick/tz/disabled) 가변화·RBAC 게이팅(§A10)·감사 로그·per-deploy 워커 cpu/mem Helm values(아래 항목, 묶을 수 있음).
- **per-deploy 워커 cpu/mem Helm values (full-plumbing)**: A3c 는 충실도 최소안으로 `WorkerResources::default()`(Guaranteed QoS, modest 250m/256Mi)를 코드 기본값으로 박고 `worker.capacityVus`(N 레버)만 Helm value 로 노출했다. 워커 req/limit 자체를 deploy 마다 Helm values 로 올리는 배선(values + controller 플래그 + dispatcher→`build_job_spec` 주입)은 연기 — `WorkerResources` 구조체·이음새가 이미 있어 순수 가산(throwaway 0). 프로덕션 고처리량 도입 시. → "운영 상한 관리자 화면"과 묶을 수도.
- ~~**`unique` 바인딩(중앙 커서)**~~ — **✅ 완료 (2026-06-02)**: 전역 커서 대신 정적 disjoint 슬라이스(`shard_split`) + stop-VU on exhaust 방식으로 구현. spec `docs/superpowers/specs/2026-06-02-unique-binding-design.md`, plan `docs/superpowers/plans/2026-06-02-unique-binding.md`, ADR-0022 갱신. 연기(이 슬라이스 내): `on_exhaust: fail` opt-in 토글.
- **컨트롤러 재시작 부분 복구**: 멀티워커 run 도 재시작 시 통째 `failed`(현 동작·fail-fast 정합). 부분 진행 복구는 안 함. → 필요 시 per-shard 상태 영속화 슬라이스.
- **open-loop 멀티워커 fan-out 연기 항목 (ADR-0038 §8, 2026-06-15)**: ① ✅ **완료 (2026-06-15)** create-time `worker_count` 사이징 헬퍼(prior-run 천장 → 권장 N을 RunDialog에서 미리; 하이브리드 3-tier — 포화 run=강한 근거/비포화=보수적 상한/근거 없음=1대 시작, `N=ceil(target×prior_wc/peak)`가 `recommended_workers`와 수식 1:1) → §A9·현재 상태 / ② per-stage 워커 분해 리포트(어느 stage에서 어느 워커가 꺾였나) / ③ 곡선 0-share 워커 floor(`worker_count > stage.target`이면 일부 워커가 그 stage에 0-share → 등록만 하고 유휴; 드문 코너) / ④ closed-loop `worker_count` 오버라이드(현재 closed는 vus/capacity로 N 유도 — open 전용 노브) / ⑤ best-effort/degraded는 위 §B2'' 항목과 동일(open-loop도 fail-fast).

### B4. Header/Form 벌크 입력 연기 항목
출처: 스펙 `2026-06-01-header-form-bulk-entry-design.md` §7 + 사용자 결정(2026-06-01).
- ~~**disabled 행 토글**~~ — **✅ 완료 (2026-06-03, master `b6d2907`)**: Postman식 KV(헤더/폼) 행을 지우지 않고 체크박스로 끄기(YAML 보존, 부하 시 미전송). 구현은 행별 `enabled: bool`이 아니라 **`Request.disabled` 사이드카**(끈 키 목록을 별도 맵에 보관) — executor가 안 읽어 byte-identical, proto/migration/워커 무변경(머지 diff = engine + ui만). UI: Zod `RequestModel.disabled`(`.optional()`) 와이어 1:1 + `normalizeRequest` passthrough(read) + `KeyValueGrid` 2-맵 계약(`onChange(active,disabled)`) + Inspector `buildDisabled` cleanup(write). subagent-driven 3 task + 최종 handicap-reviewer READY-TO-MERGE. ADR 불필요(additive). spec `docs/superpowers/specs/2026-06-03-disabled-row-toggle-design.md`, plan `docs/superpowers/plans/2026-06-03-disabled-row-toggle.md`.
- ~~**JSON body "Format/Prettify" 버튼**~~ — **✅ 완료 (2026-06-01)**: `JsonBodyField`(`Inspector.tsx`)에 Format 버튼 추가 — `JSON.parse → stringify(.,2) → setText` + 파싱값 persist(onBlur commit과 `store` 헬퍼 공유). 외부 prettier 의존 없음. RTL 3 tests.
- **(de-scoped) 멀티값 헤더(같은 key 반복)**: 사용자 판단 "헤더 중복 입력 필요성 낮음" → 후보 제외. 필요해지면 모델 확장(별도 spec).
- **raw body 에디터**: 변경 이유 없음 — 임의 텍스트라 textarea가 적합. 건드리지 않음.

### B5. codex 평가 후속 (2026-06-03)
출처: codex load-tester 평가 + Claude 검증 `docs/reviews/2026-06-02-load-tester-evaluation-assessment.md`. **항목 1–4 구현+머지 완료**(master `5e59048` lint 게이트 / `fffec3e` dispatch fail-fast P0 / `fee8041` shutdown 로그). 잔여:
- ~~**open-loop / arrival-rate 부하 모델 + per-step·per-scenario timeout (P2)**~~ — **🔜 영역으로 승격 (영역 D, 2026-06-03)**: "부하 모델·페이싱 설정"으로 brainstorm → 영역 spec `docs/superpowers/specs/2026-06-03-load-model-pacing-config-design.md`. 아래 §D 참조.
- ~~**skip/todo UI 테스트 분류·정리**~~ — **✅ 완료 (2026-06-12)**: `it.todo` 18건 전량 처분(특성화 구현 9 + 근거 명시 삭제 9), 스위트 762 passed·todo/skip 0, production 무변경(+ui/CLAUDE.md PATCH→PUT doc rot). spec/plan `2026-06-12-skip-todo-test-cleanup*`, 상세 `docs/build-log.md`. → **§B5 잔여 항목 없음(전부 완결)**.

### D. 부하 모델·페이싱 설정 (영역 D) — **✅ 완료 (S-A ✅ · S-B ✅ · S-C ✅ · S-D ✅ · closed-loop VU 곡선 ✅ — 영역 D 완결)**
- **성격**: 부하를 *어떻게 거는가*의 표현력 확장(요청 강도·페이싱·타임아웃·도착률). LoadRunner/JMeter 대체 목표(ADR-0001)의 핵심 격차. 출처 = §B5 codex P2 + 사용자 요청(target RPS / ramp stages / think time / per-step timeout / http_timeout_seconds / max in-flight).
- **영역 spec**: `docs/superpowers/specs/2026-06-03-load-model-pacing-config-design.md`. 지배 원칙: run-level knob→Profile / per-step knob→Scenario(ADR-0013), absent→byte-identical, 마이그레이션 0건(profile_json + YAML `#[serde(default)]`).
- **4 하위 슬라이스, 전부 commit, 의존성 순서 1→4** (사용자 확정: 급한 것 없음):
  - **S-A 타임아웃 ✅ 완료 (2026-06-03, subagent-driven 5 task, master `c5c3f27`→`8ff689d`)**: `http_timeout_seconds`(profile, 30s 하드코드 대체) + per-step `timeout_seconds`(HttpStep 오버라이드, reqwest `RequestBuilder::timeout`, lockstep execute_step/traced). connect_timeout은 **의도적 제외**(연기 → 아래 추가 knob). 마이그레이션 0건, absent→byte-identical, 메트릭/proto MetricBatch 무변경, ADR 불필요(additive). plan `docs/superpowers/plans/2026-06-03-load-model-pacing-s-a-timeouts.md`. handicap-reviewer READY-TO-MERGE(wire 1:1 7-layer). **이 슬라이스가 "Profile 필드 + HttpStep 필드 + proto + UI Zod + range 검증" 배선 패턴을 확립 — S-B/S-D가 재사용.**
  - **S-B think time ✅ 완료 (2026-06-04, subagent-driven 10 task, master `8ff689d`→`0dda5df`)**: 반복 간(`Profile.think_time`) + 스텝 뒤(`HttpStep.think_time`) 지연, 균등 랜덤/고정(`ThinkTime{min_ms,max_ms}`, min==max=고정). closed-loop 유지. 사용자선택 시드 `Profile.think_seed:Option<u32>`(비움=엔트로피, per-VU 재현성=`dataset::mix` 재사용) + test-run `apply_think_time` 토글(per-step만, 기본 off). 신규 `engine/src/pacing.rs`(`ThinkTime::sample` generic rng + `pace` cancel/deadline-clamp). think time은 **인터프리터**(`execute_steps`/`trace_steps`) 레이어 — executor(`execute_step`) byte-identical(애그리게이터 락은 sleep 전 drop). S-A 7-layer 재사용, **마이그레이션 0건**(profile_json serde default), absent→byte-identical, 메트릭/proto MetricBatch 무변경, ADR 불필요(additive). plan `docs/superpowers/plans/2026-06-03-load-model-pacing-s-b-think-time.md`. task별 2단계 리뷰(spec→code-quality) + 최종 handicap-reviewer READY-TO-MERGE(wire 1:1 7-layer). **머지 후 라이브 수동테스트 PASS**: 베이스라인 7341 RPS → run-level 200ms **9.99 RPS**(=2÷200ms) / per-step 150ms **13.3 RPS**(=2÷150ms), test-run off=2ms/on=804ms(800ms think 반영), UI 3표면(RunDialog Pacing+invalid 게이트 / Inspector per-step save→reload round-trip / TestRunSection 토글→trace 253ms) 전부 확인. 연기: think time Poisson 분포(S-C), constant-pacing-timer(S-C 이후), think_seed↔데이터바인딩 seed 통합. 잔여 latent flake(S-B 무관): `aggregator::tests::records_and_serializes`(wall-clock 윈도 경계).
  - **S-C 오픈루프 ✅ 완료 (2026-06-04, subagent-driven 14 task)**: 격리된 `run_scenario_open_loop`(기존 `run_scenario`/`run_vu` byte-identical, closed-loop 회귀 0) + `max_in_flight` 재사용 슬롯풀(`Vec<Arc<VuClient>>` + 사전 적재 mpsc free-index 큐, 슬롯=vu_id, jar 슬롯-지속, ADR-0018 일관) + 균등 틱 스케줄러(`tokio::time::interval`) + drop+`dropped` run-total 카운터. `dropped` 영속화 파이프라인: 엔진 `AtomicU64` → `MetricFlush.dropped`(final flush) → proto `MetricBatch.dropped=6` → 워커 forward → controller `ingest_metrics` UPDATE → migration 0009 `runs.dropped` → `ReportJson.dropped` → UI Summary. ADR-0031, 단일워커 v1(~~fan-out 연기~~ → **✅ 멀티워커 fan-out 완료 2026-06-15, ADR-0038**: 명시 worker_count + 컨트롤러 shard_split 분할, 위 §현재 상태). 노브충돌: `ramp_up_seconds`·run-level `think_time` + `target_rps` → 400. `vus` 무시(이제 `vus>0` open-loop은 400 거부 — ADR-0038 검증 ⑤). 잔여: S-D(다단계 ramp) + churn 노브 + Poisson 분포 + graceful drain(§9 연기).
  - **S-D 다단계 ramp ✅ 완료 (2026-06-05, subagent-driven 11 task, worktree worktree-s-d-stages, ff-merge 대기)**: **범위 = open-loop RPS 곡선만.** `stages: [{target, duration_seconds}]` piecewise-linear 레이트 곡선으로 S-C 고정 `target_rps` 일반화 — `stages`가 `target_rps`+`duration_seconds`의 generic 대체(상호배타, `max_in_flight` 직교 필수), `Some(vec![])=absent`. 시작 rate=0(k6 컨벤션). 엔진: `rate_at` 순수 함수(**`next`(예약 tick) 기준 평가** — `now` 기준이면 ramp 초반 ~1 arrival 버그), `run_scenario_open_loop` interval 산출만 교체(루프 body S-C 동일), rate≤1e-9=100ms poll-step, wait=deadline clamp. 워커가 `plan.duration=sum(stage durations)` 계산. 컨트롤러: `Profile::is_open_loop()`로 판별 3 사이트 통일(`target_rps.is_some()` 직접 분기 금지 — stages-only 오분류 방지). 마이그레이션 0(profile_json serde default), proto `repeated Stage stages = 10`(Profile Copy 상실→dispatch .clone()), 메트릭/MetricBatch 무변경, stages 없으면 byte-identical. UI: RunDialog 고정|곡선 토글 + stage 행 에디터(string-draft) + 부하-모양 템플릿 4종(점증/유지·스파이크·계단·소크) + Recharts 라이브 곡선 미리보기(`toControlPoints` (0,0)→누적, type=linear). `StageSchema.optional()`(default 누출 금지). ADR-0032. 라이브 e2e 검증: stages 곡선 run 298 req / 3개 1s 윈도 확인. ~~**closed-loop stages(VU 곡선+retire/ramp-down)는 별도 미래 슬라이스 연기**~~ — **✅ 완료 (2026-06-13, ADR-0037)**: `run_scenario_vu_curve` park-gate + vu_stages proto 12 + ramp_down proto 13 + UI RunDialog closed+curve 활성화. 단일워커 v1, 연기 항목 → §B9.
- **✅ 부하 모드 선택기 (UX, 2축 보존 리팩터) — 완료 (2026-06-05, subagent-driven 4 task, UI-only)**: RunDialog의 부하 모델 부분을 2축(loadModel: closed/open × rateMode: fixed/curve) **명시 셀렉터**로 다듬고 순수함수 `loadModel.ts`(`buildLoadProfile`/`loadModelErrors`) + presentational `LoadModelFields.tsx`로 추출. **closed+curve(VU 곡선)는 disabled "곧 지원"**(closed 라디오 eager `setRateMode("fixed")` + 곡선 라디오 `disabled` 이중 가드로 도달 불가; VU 곡선 실행은 별도 미래 슬라이스). `http_timeout`(3곳→1 공유) · `max_in_flight`(2곳→open 1) 중복제거. "모드당 자기 필드만 emit" 불변식 테스트로 락인. **엔진·proto·컨트롤러·마이그레이션 무변경, 제출 payload byte-identical**(라이브 3모드 검증: closed 36k req / open-fixed ~100 RPS / open-curve stages 곡선). 착수 시 발견: 영역 D 상호배타 400들은 S-C/S-D 구현 중 이미 UI 구조로 막혀 있었음 → 이 슬라이스의 가치는 버그 방지가 아니라 UX 명료화 + 코드 건강 + 불변식 락인. spec `docs/superpowers/specs/2026-06-05-load-model-mode-selector-design.md` / plan `…/plans/2026-06-05-load-model-mode-selector.md`. ADR 불필요(additive UI). (사용자 요청으로 기록 — 2026-06-04 S-D brainstorm.)
- **추가 knob 아이디어(기록, 해당 슬라이스 흡수/후속)**: connect timeout 분리(S-A에서 의도적 제외 → 후속), think time Poisson 분포(S-B/S-C), `max_iterations` cap, graceful stop/ramp-down(S-D 곡선 ramp-down은 레이트만 0으로 — in-flight grace drain은 별도), warmup 메트릭 제외, abort-on-error 실시간 threshold(A4a 사후 verdict와 별개), connection pool 튜닝(host당 max/keep-alive), per-VU rate cap(S-C 이후). 상세 = 영역 spec §8.

### B6. A4a (run-level SLO criteria) 연기 항목
- **status-class criteria** (생 4xx/5xx rate+count) — ✅ **완료 (2026-06-06, subagent-driven 7 task + a11y fix, master `8d0c28f`→`baecc24` 8 commits, ADR-0028 확장)**: `max_4xx/5xx_rate`(분수) + `max_4xx/5xx_count`(u64). rate 분모 = HTTP 응답 수(transport `"0"` 제외) = insights `status_class`와 **공유 헬퍼**(`report::http_response_total`/`status_class_count`)로 단일화. error_rate가 못 잡는 raw 4xx/5xx를 status_distribution으로 직접 평가.
- **per-window 최소 RPS** — ✅ **완료 (위 슬라이스 동시)**: `min_window_rps`(per-second 총 RPS 정상상태 최소값) + 수식자 `rps_warmup_seconds`. 첫·마지막 부분초 항상 제외 + warmup trim, eligible 부족이면 criterion skip(거짓 FAIL 금지), 전 기준 skip이면 verdict None. RunDialog closed-loop만 warmup prefill. **엔진·워커·proto·마이그레이션 무변경, 출력 스키마 무변경**(신규 metric 행 `metric:string` 자동 통과). 최종 handicap-reviewer READY-TO-MERGE(wire 1:1 6필드×6레이어) + 게이트 green(cargo workspace + UI lint/test/build) + 라이브 run(200/404/500 stub, 6455 req, verdict 5행 정확 + 실 `/report` `ReportSchema.parse` 통과 = S-D 갭 차단). spec `docs/superpowers/specs/2026-06-06-b6-status-class-window-rps-criteria-design.md`, plan `docs/superpowers/plans/2026-06-06-b6-status-class-window-rps-criteria.md`.
- **run 목록 pass/fail 배지** (fast-follow) — ✅ **완료 (2026-06-07, subagent-driven 7 task, master `695d1b3`→`bd913b6` 6 commits, ADR-0028 범위 내 additive·ADR 신규 불필요)**: 종료(Completed) 시 verdict를 `runs.verdict_json`(migration **0012**, Rust-guarded ALTER)에 영속 — 코디네이터 `Finalize::Completed` 훅이 `build_report_for_run`을 재사용(fail-soft `warn!`+skip, forward-only=백필 없음)해 **영속 verdict == on-demand `/report` verdict 불변식** 보장. read-path는 `RunRow.verdict`/`ScheduleEventRow.verdict`(관대 `.ok()` 파싱; profile_json/env_json `.unwrap()`과 의도적 비대칭) + 스케줄 events `recent_events` **LEFT JOIN runs**(컬럼 `ev_*` 별칭=sqlx 컬럼명 키 충돌 회피). 3표면 배지(run 목록 결과 열 / 스케줄 이벤트 타임라인 / run 상세 헤더 = `report.data?.verdict`)는 공유 `<VerdictBadge>`(PASS/FAIL/—, FAIL tooltip은 VerdictPanel과 공유 `fmt`=`verdictFormat.ts`) + 기존 `VerdictSchema.nullish()` 재사용(서버 `None→null`). **엔진·워커·proto 무변경.** task별 2단계 리뷰(spec→code-quality, T1 idempotency 테스트 강화·T3 `warn!`·T4 별칭 주석·T6 공유 `fmt` fold-in) + 최종 handicap-reviewer READY-TO-MERGE(wire 1:1) + **라이브 검증**(PASS+FAIL run verdict==/report·no-criteria run→`verdict:null`(`.nullish()`)·스케줄 타임라인 LEFT JOIN 배지(live cron fire)·실 바이트 throwaway `safeParse`·실브라우저 3표면 배지+FAIL tooltip `p95_ms 6 ms > 1 ms`·콘솔 Zod 0). spec `docs/superpowers/specs/2026-06-07-run-verdict-badge-design.md`, plan `docs/superpowers/plans/2026-06-07-run-verdict-badge.md`. **연기**: verdict 필터/정렬, 스케줄 요약-행 last-run verdict, baseline-상대 polarity 색.
- **step-level criteria + 일반 연산자 모델** — ✅ **완료 (2026-06-14, subagent-driven 8 task, ADR-0028 범위 내·신규 ADR 불필요)**: 가산 `step_criteria: Vec<Criterion{metric,op,threshold,target}>`(8 metric=p50/p95/p99_ms·error_rate·4xx/5xx_rate·4xx/5xx_count × op max/min, target=http-leaf step_id 필수) — fixed-field `Criteria`는 그대로, 출력 일반형에 `CriterionResult.target` 1필드만 가산. `evaluate_criteria(+steps)` step 행 평가(fixed-field 뒤 입력순 append, 미존재/`count==0` skip=거짓 FAIL 금지, 전부-skip→verdict None, rate 분모 `http_response_total`/`status_class_count` 공유헬퍼 재사용) + `validate_criteria` 범위(8-metric 화이트리스트/op/finite/rate∈[0,1] vs count≥0/non-empty) + `validate_step_criteria_targets`(`collect_http_step_ids` 재귀 walk[loop/if/parallel exhaustive·http-leaf만·container id 제외], target 미존재→400) **5 호출부 배선**(run-create·preset create/update[update는 scenario 신규 fetch]·schedule gate[create+update 공유]·schedule fire[발사마다 재검증]) + UI Zod(`step_criteria.optional()`/verdict `target.nullish()`)·`StepCriteriaFields` 행 편집기(http-leaf picker·ms/% 단위)·VerdictPanel step명+VerdictPanel/Badge React key 충돌 수정. **엔진·워커·proto·migration·export 무변경, `step_criteria` 빈·`target` None은 serde 생략→byte-identical·migration-0**. subagent-driven 8 task(task별 spec→quality 2단계 리뷰 + fold-in 4건: min-direction·schedule-gate 통합테스트·read-path %변환·key teeth-check) + 최종 handicap-reviewer READY-TO-MERGE(0 findings) + 게이트 green(cargo workspace clippy/nextest 552 + UI lint 0·test 795·`tsc -b`) + **라이브 PASS**(5ms responder: 잘못된 target/미지원 metric `rps`→400·step verdict `passed=False`[p95_ms FAIL·error_rate PASS 입력순]·무-criteria run→`verdict:null`·0 target/step_criteria 키 byte-identical·실 `/report`/run객체 `ReportSchema`(`.nullish` target)/`RunSchema`(`.optional` step_criteria) safeParse=S-D 갭 차단). spec `docs/superpowers/specs/2026-06-14-step-level-slo-criteria-design.md`, plan `docs/superpowers/plans/2026-06-14-step-level-slo-criteria.md`. **연기**: target optional(run-level 일반연산자=None→summary 평가)·richer 연산자(`==`/`<`/`>`/`!=`)·step `rps`/per-window·loop_index/branch criteria·배지 step명 plumbing·CSV/XLSX criteria 열.

### B7. A4b (run 비교 + export) 연기 항목
- **C. 트랜잭션 시간 분해 (B7-C) — ✅ TTFB+다운로드 완료 (2026-06-07)**: per-step `download` phase 분포(`p50/p95/p99/max_ms/count`)를 리포트에 노출. DNS/TCP/TLS/total phase·다운로드 SLO 기준·run 비교/insights 다운로드 통합·per-second 다운로드 시계열·다운로드 성공/오류 분리·다운로드-타임아웃 비대칭(slow download → `bytes().await Err` → `download=None`, body-read 오류만)은 **연기** — reqwest 0.12 keep-alive 재사용으로 per-request connect timing 노출 불가(침습적·저가치), v2 슬라이스에서 필요 시 재검토.
- **D. 레이턴시 히스토그램/분위 곡선** — ✅ **완료 (2026-06-05, subagent-driven 6 task, master `4c84f9b`→`370a7cb` 6 commits)**: `build_report`가 이미 머지한 `overall` HDR에서 분위 곡선(고정 11분위)+로그-버킷 히스토그램(iter_recorded 값-기준 정확 분할, Σ==len)을 `ReportJson.latency: Option<LatencyDistribution>`(`#[serde(default, skip_serializing_if)]`)로 emit → UI Zod(`.nullish()`)+`formatLatency`+Recharts 차트 2개(`PercentileCurveChart` `type=linear`·`LatencyHistogramChart` 로그-버킷 바)+ReportView `<section aria-label="Latency">` 슬롯. **엔진 부하경로·워커·proto·마이그레이션 무변경**(`percentiles.rs` 리포트 헬퍼 `percentile_curve`/`log_buckets`만 추가, executor/aggregator 미참조). task별 2단계 리뷰(spec→code-quality, fix-subagent로 tooltip formatLatency·formatLatency 경계·section a11y fold-in) + 최종 handicap-reviewer READY-TO-MERGE(wire 1:1·partition Σ==len·load-path byte-identical) + 게이트 green(cargo workspace + UI 495 pass+tsc-b) + **라이브 run 검증**(3581 req, 히스토그램 Σ==count 실데이터 확인, 실 `/report` 응답이 `ReportSchema.parse` 통과 = S-D 갭 차단). spec `docs/superpowers/specs/2026-06-05-latency-distribution-report-design.md`, plan `docs/superpowers/plans/2026-06-05-latency-distribution-report.md`. ADR 불필요(additive). **연기**: step별 드릴다운·성공/오류 분리·run 비교 곡선 오버레이·per-window 히스토그램(spec §1.2 비목표).
- **C. 트랜잭션 시간 분해**(DNS/TCP/TLS/TTFB): 엔진 계측 필요 — 더 큰 별도 슬라이스.
- **A4c 리포트 요약**: A4 영역의 별도 하위 슬라이스.
- **per-second 차트 오버레이**: 여러 run 시계열을 한 차트에(비교 뷰 후속).
- **화면 N 상한 사용자 설정화**: v1은 5 고정(서버 export 상한 50은 별개).
- **크로스-시나리오 비교**: step_id 매칭 의미 없어 범위 밖(같은 시나리오 강제).
- **verdict 행의 baseline-상대 polarity**: spec §4.3는 candidate-FAIL&base-PASS→bad를 정의하나 v1은 PASS/FAIL 텍스트(녹/적)만 렌더 — polarity 색은 미구현.
- **비교 export Δ 셀 조건부 서식**(XLSX 색): 현재 Δ%는 숫자만.

### B8. 스텝 템플릿 (2026-06-12, ADR-0036) 연기 항목
- **별도 관리 페이지 `/templates`**: v1은 삽입 모달 내 최소 관리(삭제)만. 팀 라이브러리가 커지면 EnvironmentsPage 미러(목록/이름변경/내용 미리보기/삭제)로 확장 — 사용자 결정(1안 진행, 불편 시 2안).
- **변수 파라미터화**: 삽입 시 `{{var}}`/`${ENV}` placeholder 치환 다이얼로그. v1은 as-is 복사(검증 배너·치트시트가 후속 안내).
- **컨테이너 내부 삽입**(loop/if/parallel 안으로) · **내장 스텝 템플릿** · **버전/히스토리** · **import/export** · **검색/태그**.
- **참조 기반 시나리오 조합**: 이 슬라이스로 대체된 원 아이디어 — 필요해지면 별도 spec(참조 추종·변수/쿠키 핸드오프·run 시점 합성).

### B9. Closed-loop VU 곡선 (2026-06-13, ADR-0037) 연기 항목
출처: spec §9(연기) + §10(비목표). 착수 전 이 목록을 흡수.

- **곡선 멀티워커 샤딩**: 각 stage의 target VU를 `shard_split`으로 워커 수에 분할 + 반올림 오차 설계. `max(vu_stages[].target) > capacity` 400 거부는 단일워커 전제 — capacity 초과 대형 곡선의 유일한 경로.
- **active-VU per-second 시계열** — ✅ **완료 (2026-06-14, ADR-0037 follow-up)**: 곡선 run 리포트에 초당 `{desired, actual}` 2줄 차트(목표 점선/실제 실선) — "엔진이 설계한 VU 곡선을 실제로 따라갔나"에 직접 답. 7-layer 가산: aggregator `active_vu` BTreeMap(keep-last) → `MetricFlush.active_vu_samples`(7번째 벡터, send-guard 곡선 2곳) → proto `MetricBatch.active_vu_samples=9` → migration 0016 `run_active_vu_metrics`(scalar UPSERT) → `build_report` 8번째 param → `ReportJson.active_vu_series`(독립 게이지, summary 비오염) → UI `ActiveVuChart`. 슈퍼바이저 250ms tick 샘플(핫패스 밖→처리량 무회귀), periodic 플러셔 `drain_active_vu_completed`(완료 초만). **곡선만 emit, 비-곡선 byte-identical.** 라이브: 50→0 곡선에서 desired `[3..48..2]`/actual ~1s lag 추적·rps 7.5k 무회귀·비-곡선 빈 시리즈·실 `/report` ReportSchema 파싱. 연기: CSV/XLSX 열·곡선 멀티워커 per-worker 머지·고정 closed-loop 샘플러·active-VU SLO·비교 오버레이. spec/plan `2026-06-13-active-vu-timeseries*`.
- **graceful grace 상한**(k6 gracefulRampDown류): retire 후 iteration 완료를 기다리는 최대 시간 초과 시 강제 중단 — v1은 무상한(iteration이 매우 길면 retire가 지연됨).
- **fresh-spawn 모드**: 재활성화를 같은 vu_id·jar 재사용 대신 새 vu_id·빈 jar로(신규 세션 유입 재현). v1은 park & 재사용만.
- **VU용 부하-모양 템플릿 별도 스케일**: 현재 vu_stages의 target 값 자체가 VU 수 — 모양은 유지하되 값만 선형 스케일하는 "배율" 노브 후보.
- **closed-loop 곡선 + criteria `rps_warmup_seconds` 자동 prefill**: closed 고정 VU는 ramp_up_seconds 기반 prefill 이미 있음. 곡선은 첫 stage 길이 등 휴리스틱으로 warmup 제안 가능.
- **곡선 run의 VU 표시 개선** (RunDetailPage VUs 카드 · run 목록 VUs 열): v1은 open-loop 선례대로 0 표시 수용 — "최대 N VU (곡선)" 표기 후보.

### B10. 편의(QoL) 트랙 후속 후보 (2026-06-16 표면화)
출처: 2026-06-16 "내부 테스트 단계 — 편리함에 집중" 후보 검토. **응답기반 extract 작성**(§C 후속 8-1, [[response-based-extract-authoring]])·**스텝 템플릿 관리 페이지 + 변수 파라미터화**(아래, [[step-template-management]])는 둘 다 **✅ 완료·머지 (2026-06-18)**, **ko.common 한국어화 일괄 롤업**(아래)도 **✅ 완료·머지 (2026-06-19)** — **§B10 편의 트랙 3종 전부 완결**:
- **✅ 스텝 템플릿 관리 페이지 + 변수 파라미터화 완료·머지 (2026-06-18, [[step-template-management]]·build-log)** — 전용 `/templates`(EnvironmentsPage 미러[목록/이름·설명 편집/미리보기/삭제·**생성 없음**]) + 삽입 시 `{{flow}}`/`${ENV}` 토큰별 유지/이름변경/리터럴 치환 2-phase 단일 Modal(자동 추측 0). 순수 `templateParams.ts`(Document 스칼라 visit·byte-identical 불변식) + `scanEnvVars`. `ui/`-only·엔진/proto/controller/migration 0. ADR-0036 범위 내(별도 ADR 불필요).
- **✅ ko.common 한국어화 일괄 롤업 완료·머지 (2026-06-19, [[ko-common-rollup]]·build-log)** — ui/src 잔존 영어 + 카탈로그 밖 인라인 한국어(본문·버튼·표 헤더·placeholder·`aria-label`·`title`·배너)를 `ko.ts` 경유 한국어로 **전수 이전**, half-catalog 해소(ADR-0035 단일 소스 *완성*). 신규 `ko.common`(크로스커팅 동작/상태만, R2). **정규 범위 = R1 grep 잔존 0**(§목록 아님). production byte-identical(`<option value=>`·`tab=`·enum wire값 보존, 라벨만 한국어화 — R8), 머지 diff=ui/(+docs) only. 누적 출처(전부 "ko.common 도입 시 일괄"로 연기됐던) U2/U3/U4/B7-C 영어 잔존 전부 해소. subagent-driven 6 task, handicap+security reviewer 둘 다 APPROVE, 라이브 waived(ui/-only·spec §6). 함정: implementer의 "R1 잔존 0" self-report 불신 → orchestrator가 grep 직접 재실행해 6개 추가 적발(루트 CLAUDE.md). `ui/`-only·엔진/proto/controller/migration 0.

### B3. 슬라이스 무관 tech-debt
- → **`docs/followups-after-mvp1.md` "열린 항목"** 으로 관리(열린 항목 없음 — status-transition 갭은 2026-06-05 완료). 이 로드맵 문서와 중복 적지 않는다. 잔존 후속 후보: G1a(등록 후 hung 워커 진행 라이브니스)·G1b(C — mid-run stall advisory) 완료(2026-06-23) → 잔존 B2(A/B/C 임계값 /settings 가변), G2(k8s register-전 사망 reaper, 현재 60s watchdog 폴백), run 목록 stall 배지.

---

## 사용법 (다음 세션이 봐야 할 곳)

1. **"다음 뭐 하지?"** → 이 문서 §현재 상태 + §A.
2. 슬라이스 정하면 → `superpowers:brainstorming` 으로 시작 → spec(`docs/superpowers/specs/`) → plan(`docs/superpowers/plans/`).
3. plan 작성 중 → 이 문서 §B에서 그 슬라이스가 흡수할 연기 항목을 체크(특히 control-flow면 Slice 7 ADR-0020/0021 패턴 재사용).
4. 슬라이스 끝나면 → 이 문서 §A에서 "완료"로, 새 연기 항목은 §B에 추가, 새 ADR 번호 + CLAUDE.md 인덱스 갱신.
