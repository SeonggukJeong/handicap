# 다음 작업 현황판 (roadmap-status)

> **"다음 뭐 하지?"의 단일 진입점.** 테마별 *어디까지 왔나(frontier)* + *추천 다음 작업*을 한눈에. 과거 `roadmap.md` 최상단의 next-up shortlist를 이 파일이 **대체·흡수**한다(중복 금지 — shortlist는 더 이상 roadmap.md에 없다). `/start-slice`는 이 표를 1순위로 읽고, `/finish-slice`는 끝낸 작업의 frontier를 전진시키고 추천 다음을 갱신해 항상 최신으로 유지한다(**세션마다 재합성 금지** — 손-큐레이트, 사용자 요청 2026-06-25).
>
> **상세는 여기 없다**(drift 차단): 각 테마의 후보 메뉴·착수 메모·설계 질문은 `roadmap.md §A`, 슬라이스별 의도적 연기 항목은 `roadmap.md §B`, 완료 슬라이스 이력은 `roadmap-archive.md`, 구현 결과·함정 출처는 `docs/build-log.md`. 이 표는 *어디를 펼지*만 가리킨다.

## 테마별 현황 (frontier → 추천 다음)

활성(추천 작업 있음) 3종을 위에, 성숙·완결 테마를 아래에 둔다. 성숙 테마도 자연스러운 다음 한 수를 적되 `(성숙)`으로 표시 — 당장 우선순위는 활성 3종.

| 테마 | 어디까지 (frontier) | 추천 다음 작업 | 상세 위치 |
|---|---|---|---|
| **보안·거버넌스** | 미착수 | **A10 RBAC / 기관 도입 보안 하드닝** — 코드 전에 brainstorming→ADR 선행(인증 위임[B] vs 인앱 계정[A]). 공공기관 도입 게이트라 후순위 아닌 **도입 전제**. §B1 민감값 마스킹+감사 로그 한 트랙 | roadmap.md §A10 |
| **데스크톱 배포** | 단일 self-contained exe(ADR-0039)·Tauri in-process 백엔드(ADR-0042)·NSIS 설치본 | **R4d Windows Job belt-and-suspenders**(트리거: Windows hung-워커 고아 *실측* 시만) + **코드서명**(SmartScreen 마찰)·인스톨러 메타·트레이/자동업데이트 묶음. macOS 개발기 검증 불가 | roadmap.md §A·ADR-0042·[[windows-desktop-distribution]] |
| **UX·디자인 시스템** | 토큰 토대(ADR-0043)+프리미티브 6종·4폼+결과·표시 화면군(알림→Callout·EditModal→Input) 확산·RunDialog 간단/상세 재구성 + **에디터/Inspector 화면군 토큰 이주 확산 3차**(design-system-editor: 9컴포넌트+2페이지 raw input/select/경고박스→Input/Select/Callout·토대 additive `size?:'sm'`·상속 text-xs 밀도 fold-in 2건·라이브 R16 PASS) + **Button-accent 드리프트 이주**(button-accent-migration: 컨트롤 색 리터럴 18곳 indigo/blue→accent 통일·신규 `Textarea` 프리미티브 5곳 채택[accent 포커스 링]·데이터 식별 색 5곳 동결·`accent=colors.indigo` 별칭이라 indigo→accent 픽셀 동일·라이브 computed-style PASS) + **깊은 토큰 이주 4차**(design-system-deep 2026-07-12: 신설 `PageSection`[표시 섹션 h3/h4 두 캐넌·region·className 통째-교체]+`Badge` weight/className additive — 결과·표시 화면 섹션 12곳+차트 서브 5곳+워커 배지 3곳 byte-identical·라이브 R11 전/후 computed-style 동일 PASS) + **컴팩트·카드 variant 5차**(design-system-variants 2026-07-16: Input/Select additive `compact`·`Section` `variant="card"`+aria-label+접힘 hint accname 픽스 — 동결 2곳 해동[RunListControls 날짜 컨트롤 3개 이주(radius 6px·포커스 링 획득)·Inspector `InspectorSection` 삭제+7사이트 Section화 byte-exact]·라이브 computed-style PASS) | 표시 입력 토큰화 잔여(Checkbox — 무스타일 native 2곳뿐이라 YAGNI 대기)·폼/표시 카드 지오메트리(`rounded-md p-4 bg-white` 별개 캐넌 — 수요 확인 후 variant 축 검토)·elif 카드 bespoke legend(`legendExtra` — 현재 1곳 YAGNI)·정렬 pill 투명 select(borderless 캐넌 부재)·bespoke 헤더 캐넌(headerExtra)·PageSection 화면군 밖 확산 — **5차로 동결 해동분 소진, 잔여는 전부 수요-대기(YAGNI) 성격이라 이 테마는 소강 — 다른 테마 우선 권장** | roadmap.md §B12 |
| **에디터 구조 재설계 (B13)** | 슬라이스 1(세로 아웃라인 `FlowOutline`·그룹내 dnd-kit 드래그·디테일 1fr·변수 접기·YAML 양방향 모달·React Flow 제거, ADR-0044) + **레이아웃 후속 슬라이스 A**(변수 패널 세로 스택+사용 힌트·아웃라인 http leaf 이름/URL 한 줄 truncate·YAML 모달 Monaco 높이) + **드래그 후속 슬라이스 B**(#3 컨테이너 위 드래그 취소·#4 하위 미추종 = DragOverlay 서브트리 프리뷰+그룹스코프 충돌+소스 숨김; +도그푸딩 Problem 1 헤더기반 드롭[`nearestByHeader`]·Problem 2 형제 자식 추종[노드=wrapper]) + **YAML file-I/O 후속**(편집 모달에 파일 가져오기/내보내기·`downloadJson`→`downloadText`/`downloadYaml` 일반화[JSON byte-identical]·lenient import=기존 `loadFromString` 재사용·a11y Label-in-Name) + **슬라이스 2**(테스트 흐름 칩 스트립 — `TestFlowChips` 그룹 칩 상시 미러·test-run ✓/✗/○ 결과 색·칩 클릭 선택, `chipResults`/`methodBadge` 공유 추출) + **슬라이스 3**(경계 넘는 드래그/re-parent — `dropRules` 합법성 매트릭스 13케이스·`reparentStep` 트랜잭셔널 clone-커밋·교차-밴드 인디케이터 게이트·라이브 held-drag PASS) 완료 + **공간·이름 QoL 슬라이스**(editor-space-qol: 인스펙터 5섹션 접이식[localStorage 영속]·스텝 넓게 보기 전폭 아웃라인+칩 점프+스텝 편집 모달[blur-flush]·이름 blur-Untitled) = **아웃라인 재설계 1–3 + 편집 UX 공간/이름 QoL 완결** + **변수 도구 슬라이스 A**(editor-var-tools: VariablesPanel 4상태 통합 행[선언/flat-extract/parallel-extract/미정의]·미정의 ⚠ 경고·사용-카운트→참조 스텝 순환 nav·flat 변수 rename[선언 키+extract.var+{{}} 참조 cast 보존+조건 오퍼랜드 단일 트랜잭셔널 yamlDoc 편집]·공유 scanVars 분석 토대[splitFlowToken cast-base/produced/namespaced/ref 인덱스]·라이브 3/4 거동 실측) + **변수 도구 슬라이스 B**(editor-var-tools-b: parallel-분기 extract 변수 scope-aware rename — 분기 서브트리 bare {{var}}+다운스트림 {{branch.var}} branch-스코프 재작성[lookahead splitFlowToken 대칭·CAST_KEYWORDS import]·non-shadow만 rename 활성[shadow=배지 "이름 충돌"]·(branch,var) 구조적 identity·enriched nav[분기내부∪다운스트림]·editor-time Playwright PASS) + **변수 패널·뷰포트·rename UX 폴리시**(editor-varpanel-viewport-polish: #3 사용처 nav 팝오버[portal-fixed body·overflow-auto 열 미클립·edge-flip·순환 nav 대체]·#4 뷰포트 높이 캡[max-h-[calc(100vh-16rem)]+grid-rows-[minmax(0,1fr)]·min-h-[680px] 제거]+열별 내부 스크롤·aside overflow-visible로 HelpTip 미클립·#5 rename 열린 스텝 draft 3종 실시간 재시드[store renameEpoch·self-commit 무재시드]·#6 변수 검색[이름·값·분기표기 부분일치·표시전용]·라이브 getBoundingClientRect/input.value all-PASS) = **아웃라인 재설계 1–3 + 편집 QoL + 변수 도구 A·B + UX 폴리시 + **뷰포트 높이 floor**(editor-viewport-height-fix: min-h-[520px] floor+변수 스크롤바 gutter) + **뷰포트 폴리시 v2**(editor-viewport-polish-v2: A 얇은 스크롤바·E 1줄입력 스크롤 제거·D 팝오버 뷰포트 clamp·C sticky 접이식 크롬[chromeCollapsed]·B 변수 넓게 뷰모드[varsWide] + 라이브 도그푸딩 fix 2[AutoGrowTextarea ResizeObserver 폭-재계산·그리드 max-h→h fill-height로 패널 한 화면]) + **추출 변수 이름 가시성**(extract-var-name-visibility: 추출/미정의 행 flex-wrap+이름 min-w-[72px]·usage 버튼 text-left 좌측 통일) 완결** + **저장 안 됨 이탈 가드**(unsaved-changes-guard 2026-07-12: `useUnsavedGuard` useBlocker 공유 훅+3/2버튼 공유 다이얼로그+beforeunload·에디터 테스트 하니스 createMemoryRouter 이주 — 연기 §B18) + **변수 충돌 배지·원클릭 선언**(editor-var-conflict-quickadd 2026-07-12: 선언 행 amber "추출 덮어씀" 배지[flat∪namespaced 판정·조건형 title]·⚠ 미정의 행 원클릭 "선언 추가"·yamlError 게이트 fold — §A12 도그푸딩 1호) + **시나리오 기본 think time**(think-time-defaults 2026-07-13: 루트 default_think_time을 http 스텝이 상속·스텝 3상태[상속/override/{0,0} 대기없음]·parallel 분기 배제[runner/trace Parallel arm None]·사이징 패리티[슬롯 과소추천→부하 divergence 방지]·proto/controller 0-diff — §A12 도그푸딩 2호) | bulk 변수 편집(BulkEditPanel+kvBulk 확장)·FlowOutline 스텝-레벨 미정의 배지·producer 스텝 nav·position-aware shadow rename·shadow flat+parallel flat-identity 행 복원·merge/오타-재연결 rename — roadmap.md §B15·§B16(§7 연기 항목: 검색 고급[정규식/스코프/하이라이트]·팝오버 화살표키 내비/포커스 트랩·#4 전체화면 셸·HelpTip/헤더메뉴 portal화·구조편집 draft 재시드[fieldRewriteEpoch 일반화]), brainstorming 선행 + **test-infra 함정**(suite-wide 비결정 격리 flake: setup.ts afterEach(cleanup) 추가+localStorage 폴리필 파일간 리셋) + **도그푸딩 §A12**(HAR host-환경 힌트·에디터 test-run — editor-space-qol에서 미채택 보존; **think time 일괄 지정 = think-time-defaults로 완료** 2026-07-13; **데이터셋 미리보기 = dataset-preview로 완료** 2026-07-16[도그푸딩 3호]) — re-parent 완료로 flat-tree Approach B 전면재작성 불필요 확정(R10 밸브: held-drag 취소 재발 0) | roadmap.md §B13 |
| **부하모델·페이싱 (영역 D)** | S-A 타임아웃·S-B think·S-C open-loop·S-D stages·closed-loop VU 곡선 + 곡선 멀티워커 샤딩 + §B21 open-loop think 검증/무시 토글 + **§B9 graceful ramp-down 상한**(graceful-ramp-down-cap 2026-07-16: 은퇴 VU가 cap(초) 초과 lingering 시 supervisor `retire_expired`가 activation 토큰 취소→스텝 경계 park·opt-in None=무상한 byte-identical·proto 14/store/validate 3거부/UI 프리필+canSubmit 게이트·라이브 capped 29<uncapped 36·머지 40021bf) — **영역 D 완결** | (성숙) §B9 잔여 QoL — **fresh-spawn 모드**(재활성 시 같은 vu_id·jar 재사용 대신 새 vu_id·빈 jar) / **VU 배율 노브**(vu_stages 모양 유지·값만 선형 스케일). 둘 다 엔진 supervisor 재활성 경로 변경이라 brainstorming 선행. 잔여 §B22 nit 2건은 record-only | roadmap.md §D·§B9·ADR-0031/0032/0037 |
| **리포트 깊이 (A4)** | SLO criteria(A4a)·run 비교+CSV/XLSX(A4b)·인사이트(A4c)·latency 분포(B7-D)·TTFB+다운로드(B7-C)·비교 뷰 깊이/polish·XLSX Δ 조건부 서식(Summary+Steps 완료, 2026-06-26) | (성숙) B7 심화 — Steps 시트 per-step p50/p99+error-rate Δ 확장(작은 백엔드, 시트 재설계 필요)·트랜잭션 분해 DNS/TCP/TLS(큰 엔진 슬라이스) | roadmap.md §A4·§B7·ADR-0017/0028/0030 |
| **분산 워커** | fan-out(A3·ADR-0027)·open-loop fan-out(ADR-0038)·LAN pool L1~L7(ADR-0041)·풀 견고성/제어상태 영속화 | (성숙) capacity-aware 풀 (L2 — 워커 용량 광고·존중; 현재 capacity 무시) | roadmap.md §A3·§B2''·ADR-0041 |
| **용량·사이징 인사이트 (A9)** | 포화 인사이트 v1·Little's Law 사이징·create-time VU/슬롯/worker_count 헬퍼·open-loop misconfig 경고 + **open-loop 사이징 교정 슬라이스 ①**(ADR-0046 2026-07-12: target=반복/초 공식화·실측 hold 사이징·cause 2-way·헬퍼 4앵커[포화 run hold 복원=R9 서버 parity]) + **슬라이스 ② 단위 표면화**(open-loop-rate-labels 2026-07-12: "도착률(초당 반복)" 라벨 개명·"≈ 초당 요청 N건" 환산 힌트·리포트 목표/달성 도착률 카드+헤드라인·RPS 스윕 keep-list-only — **ADR-0046 UI 표면 완결**) | (성숙) §B20 잔존 — 목표/달성 도착률 시리즈+per-second dropped 시리즈(동반 필수·와이어 추가 필요)·loadgen cause 재도입(워커 텔레메트리 proto)·곡선 prior 워커 앵커(UI 적분 복제) — 전부 와이어/proto 비용, 수요 시 착수 | roadmap.md §B20·§A9·ADR-0046/0028 |
| **제어흐름 노드** | loop(7)·conditional(A1·ADR-0023)·parallel(A2·ADR-0033) + 그룹/페이지·per-branch 레이턴시 | (성숙) per-branch phase(TTFB/다운로드) 3차원·중첩 parallel(저우선) | roadmap.md §A1/§A2·ADR-0020/0023/0033 |
| **운영·스케줄러** | Run 스케줄러(A7·ADR-0034)·운영 상한 관리자 v1·설정 환경 그룹핑(B11) | (성숙) 알림 레이어(이메일/슬랙/웹훅, 이음새=`schedule_events`)·스케줄러 config 런타임 가변 | roadmap.md §A7·§B11·ADR-0034 |
| **템플릿·캐스트** | JSON 바디 캐스트 ADR-0029 v1 + env/시스템 토큰·`:json`(객체/배열/숫자/불리언/문자열/null·변수기반 null) 확장 완료 (json-cast-extend) | (성숙) form/쿼리/raw 바디 캐스트(와이어 전부 문자열=검증-only 가치·저우선)·nullable 규칙(`:json?` empty/unbound→null)·**env-secret WARN-로그 노출 마스킹은 §B1 트랙** | roadmap.md §B1·ADR-0029 |

**소규모 후속**(위 테마에 안 묶이는 자잘한 것 — 출처 §B*): G2 k8s register-전 reaper(현 60s watchdog 폴백) · best-effort/degraded 모드 토글(§B2'') · legacy EditPage 테스트(save/clone/testrun) StrictMode 렌더 통일. ~~per-worker p95/p99 분해~~ ✅ 완료(2026-06-25, noncurve-fanout-worker-display — `WorkerBreakdownTable`이 곡선/비곡선 fan-out 공통 커버). ~~RunDialog 크기 칩 상대배수 사이징(Option C)~~ ✅ 완료(2026-07-09, rundialog-size-chip-multiplier — 고정 10/50/200 대신 직전 완료 closed-loop run 대비 0.5×/1×/2×, 없으면 고정 3개 폴백). → 착수 슬라이스 plan 작성 시 `roadmap.md §B`에서 흡수.

## 갱신 규칙 (finish-slice가 손본다)

- 끝낸 작업: 해당 테마 행의 **frontier에 한 마디 추가** + **추천 다음을 새 frontier 기준으로 교체**(다 끝났으면 `(성숙)` + 다음 자연수 한 수).
- 새 활성 후보 등장: 성숙 테마의 추천 다음을 활성으로 끌어올리거나, 새 테마 행 추가.
- 이 파일엔 *현황·포인터*만 — 구현 상세·완료 로그·연기 항목 카탈로그는 절대 쌓지 말 것(각각 build-log·roadmap-archive·roadmap.md §B). 비대 신호 = 한 행이 두 줄을 넘으면 상세를 roadmap.md로 밀고 포인터만 남긴다.
