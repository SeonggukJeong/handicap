.superpowers/
/target
*.db
*.db-journal
*.db-shm
*.db-wal
.DS_Store
ui/node_modules
ui/dist
ui/.vite
ui/coverage
.claude/worktrees/
# Playwright MCP run artifacts + stray screenshot captures (live-verification
# leftovers that finish-slice used to clean by hand). Root-only for *.png so
# committed doc images under docs/ are unaffected.
.playwright-mcp/
/*.png
# cargo-mutants audit reports (see /mutation-audit skill) — never committed.
mutants.out/
mutants.out.old/
